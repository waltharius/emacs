#+title: Function Helper
#+subtitle: Personal reference for custom Emacs functions and transient menus
#+startup: overview
#+options: toc:2 num:nil
#+property: header-args :eval never-export

* Notes Menus (C-c n)
:PROPERTIES:
:CUSTOM_ID: menu-notes-main
:END:

Main entry point for all note operations. Hierarchical structure:
=c= Create, =f= Find, =i= Insert, =d= Document, =x= Export,
=p= Writing projects (dynamic), =v= View, =t= Tools,
=l= Philosophy (dynamic), =z= Zettelkasten (dynamic),
=h= Function Help. Also carries
two top-level shortcuts duplicated from Tools → Spelling for the two
most-used spelling commands: =s= Correct previous, =a= Add to dict
(both =:transient t=, so the menu stays open after use). Both also have
direct global bindings that skip this menu entirely — =C-c s= and
=C-c S=, see [[#global-keybindings][Global Keybindings]].

=l=, =z= and =p= are not defined in =12-transient.el= itself: they are
appended at load time by =19-philosophy-notes.el=,
=22-zettelkasten.el= and =28-writing-projects.el= respectively, which
is why all three modules must load *after* =12-transient.el=. They go
through =my/transient-append=, which reports and skips instead of
signalling when the prefix, the anchor key or transient itself is
missing — so removing any one module leaves the rest of the menu
intact.

Module: =modules/12-transient.el=

** C-c n c — Create / Capture
:PROPERTIES:
:CUSTOM_ID: menu-notes-create
:END:

Create notes and manage captures. Note types in the first column,
capture commands in the second. <<p-1>>

*** n — New note
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-base
:END:

Creates a plain Denote note. Prompts for title, keywords and silo
(=pks= / =docu= / =journal=). The keyword prompt completes against the
keywords already in use and takes entries separated by commas — see
[[#keyword-prompts][Keyword Prompts]].

Afterwards =my/note-add-front-matter-extras= appends =#+language: pl=
and =#+schema:= to the front matter Denote generated. Denote's own four
lines are left to Denote: overriding =denote-org-front-matter= would put
this configuration on the hook for keeping that format string in step
with upstream, whereas Denote's rename and refresh functions rewrite
their own lines individually and leave unknown keywords alone.

=#+language:= is written as =pl= without asking. A note in another
language is not rare enough to justify a prompt on every creation and
not common enough to be worth getting wrong by default; the indexer
verifies the declared language against a detector and logs
disagreements, which is the right place for that check.

Function: =my/denote-base= | Module: =modules/05-notes.el=

*** j — Journal today
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-journal
:END:

Opens today's journal. If it exists, appends a new timestamped heading
(=* HH:MM=) at the bottom. If not, creates a new file in =~/notes/journal/=
with front matter only: =#+title:=, =#+date:=, =#+filetags:=,
=#+identifier:=, =#+language:=, =#+schema:=.

No metrics and no placeholder for them — see [[#fn-my-journal-set-metrics][w — Metrics today]]. There
have been three formats:

- *Schema 0*, before 2026-08: a =:PROPERTIES:= drawer holding
  =:well-being:= directly below the front matter. Org does not parse a
  property drawer in that position — a pre-headline property block must
  sit at the very top of the buffer with only comments above it — so
  those values were never properties and =org-entry-get= returned nil.
- *Schema 1*, for about a day: a drawer under a =* Metryki= headline.
  Parsed correctly, but it put a second =:PROPERTIES:= block in files
  that already had one under =* Uzupełnienie=, and a duplicated metrics
  block appeared in practice.
- *Schema 2*, current: front-matter keywords. A keyword name is unique
  in the file and has no positional rules.

All three are readable; only schema 2 is written.

Function: =my/denote-journal= | Module: =modules/05-notes.el=

*** J — Journal date
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-journal-date
:END:

Creates or opens a journal for an arbitrary date (backdating). Uses
=org-read-date=. Existing journal gets a =* Uzupełnienie= heading with
=:ADDED_AT:= / =:EVENT_DATE:= properties. New backdated files use a
=T000000= identifier — a zeroed clock is this configuration's marker for
"the time of day was not recorded", and journal notes are the only ones
ever created that way, so no collision search is needed.

File creation lives in =my/denote-journal--create-backdated=, which
writes the file and returns its path without visiting it. That helper is
also what =my/journal-set-metrics-for-date= calls when it is asked for a
day that has no journal at all. It refuses to overwrite an existing
file.

Function: =my/denote-journal-date= | Module: =modules/05-notes.el=

*** w — Metrics today
:PROPERTIES:
:CUSTOM_ID: fn-my-journal-set-metrics
:END:

Prompts for the metrics of the journal file in the current buffer and
writes them as front-matter keywords. Fields are declared in
=my/journal-metrics-fields=; as of schema 2 they are =#+wellbeing:=
(1–10), =#+alcohol_u:= and =#+illness:=. Deliberately short: everything
WHOOP already records (sleep, caffeine, recovery, the behaviour
checkboxes) is imported from its CSV export instead of being typed
twice.

Answering a prompt with =RET= accepts the pre-filled value: whatever is
already recorded in any of the three formats, then the field default
(=0= for alcohol, =none= for illness). Answering =-= removes the
keyword. A blank value is never written — an absent keyword means "not
measured" and must never be read as zero. The distinction earns its keep
when backfilling: =#+alcohol_u: 0= means "I did not drink", no keyword
means "I do not remember whether I did".

Three keywords are written without being asked for:

- =#+metrics_added:= — the day the metrics were *first* recorded, never
  refreshed on later edits. Date only, no clock time: the indexer
  computes a lag in days from it, and a minute resolution is finer than
  the unit of analysis. A few files from the first version still carry
  a time; the parser takes the date part, so they need no rewriting.
- =#+recalled:= — =t= when well-being is written for a day other than
  today *and* the value differs from whatever was there before. Set
  once, never unset. It exists to be filtered on, not weighted: nothing
  here could calibrate a decay curve for recalled ratings.
- =#+no_entry_reason:= — offered only for a day *other than today* that
  has no prose (below =my/journal-prose-threshold= characters), from
  =my/journal-no-entry-reasons=. Today's entry is never missing: the day
  is not over, and the question is absurd on a journal created seconds
  ago. Explanatory only in any case — the word count computed at index
  time stays authoritative for whether a day was written up.

*Three phases: read, prompt, write.* Existing values are gathered first,
then every prompt is answered into memory, and only then is the buffer
touched — so =C-g= at any point leaves the file byte-identical. The
schema 1 implementation created its headline before prompting and did
not have this property.

A note in an older format is converted on the way through: the existing
value seeds the prompt, and the write phase removes the =* Metryki=
subtree or the legacy drawer and stamps =#+schema:=. Nothing is migrated
in bulk by this command — see [[#fn-my-journal-migrate-metrics-format][my/journal-migrate-metrics-format]] for that.

Function: =my/journal-set-metrics= | Module: =modules/05b-journal-metrics.el=

*** M-x my/journal-migrate-metrics-format
:PROPERTIES:
:CUSTOM_ID: fn-my-journal-migrate-metrics-format
:END:

Batch conversion of journal files from schema 0 or 1 to keywords. Not on
a key: it is run once, deliberately, with the notes repository clean.

Pure format translation — every value is carried across unchanged and
nothing is invented, so it needs no prompting. A file whose legacy
drawer was empty becomes a file with no metrics keywords, not one with
empty ones.

Without a prefix argument it is a *dry run*: it lists what it would
convert in =*journal-migration*= and writes nothing. =C-u= writes.
=C-u C-u= additionally converts schema 0 notes — that is the
3500-file case and produces one very large commit, so by default only
schema 1 files are touched, of which there are a handful.

Function: =my/journal-migrate-metrics-format= | Module: =modules/05b-journal-metrics.el=

*** M-x my/journal-metrics-reminder
:PROPERTIES:
:CUSTOM_ID: fn-my-journal-metrics-reminder
:END:

Says in the echo area that today's journal has no well-being recorded
yet, and returns non-nil when it did. Called by [[#fn-my-denote-journal][j — Journal today]] when
appending to an existing entry, in place of its usual confirmation
message; guarded with =fboundp=, so removing the metrics module removes
the nudge.

Deliberately not a prompt, and deliberately not on the path that creates
the file. =WELLBEING= is defined as an assessment of the whole day made
in the evening; asking for it at the top of the morning entry would
reliably produce a morning mood wearing a whole-day label, and binding
it to =C-c n c j= would re-ask on every append during the day. A message
that appears when you come back to the file later costs nothing and
arrives at roughly the right time.

Function: =my/journal-metrics-reminder= | Module: =modules/05b-journal-metrics.el=

*** M-x my/notes-normalize-front-matter-gap
:PROPERTIES:
:CUSTOM_ID: fn-my-notes-normalize-front-matter-gap
:END:

Puts exactly one empty line between the front matter and the body in
every note across all three silos.

Needed because removing the schema 0 drawer takes its trailing blank
line with it, leaving the first prose headline welded to the last
keyword. `my/journal-set-metrics` and the batch converter both call the
same normalizer on write, so files touched from now on come out right;
this command fixes the ones already converted.

Each file is read, fixed and written on its own, so an interruption
leaves the files already done in a good state rather than a half-written
batch. Files that already have the right spacing are not rewritten at
all, which keeps modification times and the diff down to what actually
changed.

Dry run without a prefix argument, =C-u= to write. Not on a key.

Function: =my/notes-normalize-front-matter-gap= | Module: =modules/05b-journal-metrics.el=

*** W — Metrics for date
:PROPERTIES:
:CUSTOM_ID: fn-my-journal-set-metrics-for-date
:END:

Asks for a date with =org-read-date=, opens that day's journal and runs
[[#fn-my-journal-set-metrics][w — Metrics today]] on it. If no journal exists for that day, offers to
create one via =my/denote-journal--create-backdated=. This is also the
way an old entry is upgraded to the current format while its well-being
value is being reviewed.

Creating it is the point: a day rated with no prose is still a valid
observation, and refusing to create the file would leave a hole in the
series with no record of why it is there. The cursor is left at the end
of the buffer afterwards, which is the cheapest available prompt to
actually write the day up.

Function: =my/journal-set-metrics-for-date= | Module: =modules/05b-journal-metrics.el=

*** e — Essay
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-essay
:END:

Creates note prefixed =ESEJ:= with keywords =esej=, =project=, plus one
or more project keywords chosen at the same completing prompt as
everywhere else ([[#keyword-prompts][Keyword Prompts]]) — the project keyword is the one
that must match existing notes exactly, since it is what later gathers
the essay's material. Inserts scaffold: Metadata, Essay Plan,
Bibliography, Working Notes. Always saved in =pks= silo.

Function: =my/denote-essay= | Module: =modules/05-notes.el=

*** L — Linked note
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-linked-note
:END:

Creates a new note linked bidirectionally to the current buffer.
Keywords come from the same completing prompt as everywhere else
([[#keyword-prompts][Keyword Prompts]]). Requires =#+identifier:= in source note. Source gets a forward link,
new note gets a =:BACKLINK:= property. Layout: source left, new note
right in split window.

*Warning:* only works from a Denote =.org= file with =#+identifier:=.

Function: =my/denote-linked-note= | Module: =modules/05-notes.el=

*** i — Ideas capture
:PROPERTIES:
:CUSTOM_ID: fn-my-capture-idea
:END:

Fires capture template =j= directly, no menu. Opens capture buffer to
the right of current window. Records =:SOURCE:= property automatically.

Confirming with =C-c C-c= asks which existing capture heading the entry
belongs to — see [[#capture-filing][Capture Filing]]. The default is to keep it as a new
heading, so a plain =RET= at the prompt behaves as before.

Function: =my/capture-idea= | Module: =modules/06-capture.el=

*** c — Capture menu
:PROPERTIES:
:CUSTOM_ID: fn-org-capture
:END:

Standard =org-capture= menu with all templates. Also global =C-c c=.

Function: =org-capture= (built-in)

*** m — Promote to note
:PROPERTIES:
:CUSTOM_ID: fn-my-capture-promote-to-note
:END:

Promotes heading at point in captures file to a new Denote note.

A capture heading may hold material from several origins, gathered there
by =C-c C-c= over successive captures. Each is carried across with its
own =Source:= line, in capture order, and a line is written only when
the origin changes — including against the line the target note already
ends with. See [[#capture-filing][Capture Filing]].

*Warning:* destructive — deletes the original heading, no automatic undo.
Commit captures.org before running on important entries.

Function: =my/capture-promote-to-note= | Module: =modules/06-capture.el=

** C-c n f — Find / Search
:PROPERTIES:
:CUSTOM_ID: menu-notes-find
:END:

Search, navigate, and review notes. =f/g/b= locate specific notes;
=d/t/r= give overview/serendipity views; =h= opens historical
dashboards (dynamic submenu).

Module: =modules/12-transient.el=

*** f — Find file
:PROPERTIES:
:CUSTOM_ID: fn-denote-open-or-create
:END:

Opens existing note by completion, or creates new one if no match.

Function: =denote-open-or-create= (Denote package)

*** g — Grep notes
:PROPERTIES:
:CUSTOM_ID: fn-consult-denote-grep
:END:

Full-text search across notes with live preview.

Function: =consult-denote-grep= | Module: =modules/04-denote.el=

*** b — Backlinks
:PROPERTIES:
:CUSTOM_ID: fn-denote-backlinks
:END:

Shows all notes linking to the current note.

Function: =denote-backlinks= (Denote package)

*** d — Dashboard
:PROPERTIES:
:CUSTOM_ID: fn-my-open-notes-dashboard
:END:

Opens the notes overview dashboard in its own tab-bar tab named
"Dashboard", creating the tab if it does not exist. Seven sections
arranged into side-by-side columns; each column is a separate window,
so they scroll independently.

Default layout:

| Column | Sections                              |
|--------+---------------------------------------|
| left   | Recently Modified, Journal, Documentation |
| right  | PKS, HUB, Zettelkasten, Tags          |

**** Sections are data

=my/dashboard-sections= maps a symbol to the function that renders the
section; =my/dashboard-columns= says which symbols go in which column.
Rearranging the dashboard is an edit to a list of lists, not to the
renderer:

#+begin_src emacs-lisp
;; back to the single wide column of the old layout, tags on lines
(setq my/dashboard-columns '((recent journal pks docu tags)))
#+end_src

A symbol named in =my/dashboard-columns= but absent from
=my/dashboard-sections= renders as a visible "Unknown section" header
rather than signalling, so a typo costs one line and not the dashboard.
Column buffers left over from a wider layout are killed on refresh.

**** Why two columns and not three

The line format is two spaces, a ten-character date, a five-character
signature field and the title — nineteen characters before the title
starts. On a ~130-column frame three columns leave about twenty-three
characters for the title, which truncates ordinary entries like
"Chrześcijaństwo - religia niewolników". Two leave about sixty-six,
which is comfortable.

Lines are truncated rather than wrapped (=truncate-lines= is set by
=my/dashboard-mode=): a title too long for its column is cut at the
window edge instead of folding onto a second line and breaking the
alignment of everything below it. The full path is in the tooltip.

Per-line tags are dropped whenever there is more than one column. They
start at =my/dashboard-tag-column= (92) and no split column is that
wide; they remain reachable through the Tags section. A one-column
layout gets them back automatically.

**** Line format

: <date>  <signature>  <title>                    :tag:tag:

- *Date* is modification time for Recently Modified, PKS, Docu, HUB and
  Zettelkasten; for Journal and tag popups it is the creation date
  parsed from the Denote identifier, since chronological order is what
  matters there.
- *Signature* is the Folgezettel component of the file name (see
  [[#menu-zettelkasten][Zettelkasten]]), padded to
  =my/dashboard-signature-width= so that titles line up in sections
  mixing sequence notes with plain ones. Notes without a signature
  leave it blank.
- *Title* comes from =#+title:= in the front matter.
- *Tags* come from =#+filetags:=, shown from column
  =my/dashboard-tag-column= onwards, and only in a one-column layout.

Date, signature and title form the clickable region; tags sit outside
it so they can keep their own dimmed face rather than inheriting the
button's. Clicking opens the note in a new tab, unchanged by the column
layout.

**** The HUB and Zettelkasten sections

*HUB* lists notes carrying the =hub= keyword in their file name, across
every silo. What counts as a hub is owned by =33-denote-hubs.el= (see
[[#fn-my-denote-add-to-hub][Add to HUB]]), so the section calls
=my/denote-hub-files= when that module is loaded and falls back to its
own file-name scan when it is not. Resolution happens at call time, so
the load order of the two modules does not matter and neither requires
the other.

*Zettelkasten* lists notes carrying a Denote signature — the =...==SIG--=
component — sorted by modification time. The filter is a regexp over
file names, so only the notes that pass it are ever asked for their
modification time, which is what keeps the section cheap across
thousands of files.

Both are name-only scans and cost nothing measurable. The section that
does cost is *Tags*: a tag lives in the front matter, so
=my/denote-all-tags= opens every file in the collection on every
refresh. That is unchanged, and it is what a slow refresh is.

**** Recently Modified is capped

=my/dashboard-recent-limit= (20) bounds the section; when entries are
hidden, a dimmed line says how many, so a truncated list is never
mistaken for a short one.

The cap is the fix rather than a workaround. Modification time stops
witnessing "what have I been working on" after a bulk operation:
rewriting front matter across the collection stamps every file with the
same afternoon and buries the answer under thousands of lines. The date
of the last commit touching a file would be the honest signal, but that
is one =git= subprocess per file, on every refresh, across thousands of
files. Set the variable to nil to remove the cap.

**** Tuning

| Variable                       | Default              | Effect                                   |
|--------------------------------+----------------------+------------------------------------------|
| =my/dashboard-columns=         | two columns          | Which sections go in which column         |
| =my/dashboard-recent-days=     | 10                   | Age of the oldest file in Recently Modified |
| =my/dashboard-recent-limit=    | 20                   | Cap on Recently Modified (nil = no cap)  |
| =my/dashboard-journal-limit=   | 10                   | Cap on the Journal section               |
| =my/dashboard-silo-limit=      | 20                   | Cap on PKS, Docu, HUB and Zettelkasten   |
| =my/dashboard-tag-limit=       | 30                   | Cap on the Tags section                  |
| =my/dashboard-signature-width= | 5                    | Width reserved for the signature         |
| =my/dashboard-show-tags=       | =t=                  | Tags on lines (one-column layouts only)  |
| =my/dashboard-tag-column=      | 92                   | Column where the tag list starts         |
| =my/dashboard-hidden-tags=     | =("journal" "docu")= | Tags suppressed as uninformative         |

One =my/dashboard-silo-limit= covers four sections because they are the
same kind of list — the most recently touched notes of a set — and four
separate options would be four things to keep in step for no gain.

=my/dashboard-hidden-tags= exists because the Journal and Documentation
sections are already labelled: repeating =:journal:= on every line of
the Journal section adds nothing and pushes the informative tags
further right.

**** Keys inside the dashboard

=my/dashboard-mode= derives from =special-mode=, which supplies the
read-only buffer that used to be arranged by hand with
=read-only-mode= and =local-set-key=. One mode for every column is what
keeps the columns behaving identically.

| Key   | Effect                                          |
|-------+-------------------------------------------------|
| =g=   | Refresh: re-render every column, rebuild layout |
| =q=   | Close the columns and bury the dashboard        |
| =RET= | Open the note or tag on the current line        |

=q= is =my/dashboard-quit=, not =bury-buffer=: burying alone would
dismiss only the column point happens to be in and leave the others on
screen.

**** Performance note

Each dashboard line needs both the title and the tags of a file.
=my/denote-file-title= and =my/denote-file-tags= each open the file
separately, so the renderer uses =my/denote-file-metadata= instead,
which extracts both from a single 800-byte read. The two single-purpose
helpers remain for callers that need only one of the values.

Functions: =my/open-notes-dashboard=, =my/render-notes-dashboard=,
=my/dashboard--render-column=, =my/dashboard-insert-file-link=,
=my/denote-file-metadata=, =my/denote-file-signature=,
=my/denote-hub-notes=, =my/denote-signature-notes= |
Module: =modules/15-workspace.el=

*** t — Tag stats
:PROPERTIES:
:CUSTOM_ID: fn-my-notes-explore
:END:

Tag usage statistics across the notes collection.

Function: =my/notes-explore= | Module: =modules/15-workspace.el=

*** r — Random note
:PROPERTIES:
:CUSTOM_ID: fn-denote-explore-random-note
:END:

Opens a random note from the collection. Useful for serendipitous
review and rediscovery.

Function: =denote-explore-random-note= (denote-explore package)
*** h — History →
:PROPERTIES:
:CUSTOM_ID: menu-notes-history
:END:

Dynamically appended by =21-dashboards.el= after =12-transient.el=
loads (via =transient-append-suffix=), the same mechanism used by
Philosophy (see [[#menu-notes-philosophy][Philosophy]] below). If the
module is removed from =init.el=, this entry disappears automatically
without editing =12-transient.el=.

Both dashboards read the Denote =#+identifier:= (=YYYYMMDDTHHMMSS=)
from front matter, not the file name directly, and scan across all
three silos. Journal notes are recognised by =my/dashboards--journal-p=
from the =journal= Denote keyword in the file name, which costs no file
access at all; a note whose name lost its keywords falls back to a
content check for any of the three metrics formats — =#+wellbeing:=,
the =* Metryki= headline, or the legacy =:well-being:= drawer.

Until 2026-08 the only test was the =:well-being:= drawer. That marker
is being retired: a note migrated to the metrics drawer no longer has
it, so the old test would have quietly stopped recognising both migrated
and newly created journals, and the dashboards would have shown them as
ordinary notes.

**** Layout: own tab, list left, note right

Invoking any of the four commands switches to a tab-bar tab named
=my/dashboards-tab-name= ("History"), creating it beside the current
tab if absent (no positional argument is passed to =tab-bar-new-tab=,
and =tab-bar-new-tab-to= defaults to ='right=). The tab is reduced to
a single window holding =*Note History*=, so the layout is predictable
whatever the tab contained before.

Selecting an entry splits the window and shows the note on the right,
with the list keeping =my/dashboards-nav-width= columns on the left.
Every subsequent selection reuses that same window, so clicking
through the list replaces the note rather than accumulating windows.
Point stays in the list.

The preview window is remembered in the buffer-local
=my/dashboards--preview-window= and validated with =window-live-p=
before each use, so closing it (=C-x 1=, switching tabs) is harmless:
the next selection simply splits a new one.

***** Keyboard navigation

Inside =*Note History*= (major mode =my/dashboards-nav-mode=, derived
from =special-mode=):

| Key   | Effect                          |
|-------+---------------------------------|
| =n=   | Next entry, preview it          |
| =p=   | Previous entry, preview it      |
| =RET= | Preview entry on current line   |
| =o=   | Same as =RET=                   |
| =q=   | Bury the buffer (=special-mode=) |

=hl-line-mode= is on so the current entry is visible while paging.

***** Why =switch-to-buffer= rather than =set-window-buffer=

=my/dashboards--visit= wraps =switch-to-buffer= in
=with-selected-window= instead of calling =set-window-buffer=
directly. Only the former records the outgoing buffer in that
window's history, which is what makes =C-x <left>= (=previous-buffer=)
and =C-x <right>= (=next-buffer=) step back through previously
previewed notes.

Note that those two commands act on the *selected* window. Since
point stays in the list after a selection, move into the note window
first (=C-x o= or a click) for them to cycle notes. They also walk
that window's entire buffer history, not only notes reached from this
list.

***** Limitation: the preview window is not dedicated

Nothing marks the preview window as reserved, so an unrelated
=display-buffer= call may reuse it. Marking it dedicated would prevent
that but would also stop =find-file= from working inside it, which is
the more common need — reading a note usually leads to following a
link out of it.

Module: =modules/21-dashboards.el=

**** This Day in History (years)

| Key | Function                                     | Description                                        |
|-----+----------------------------------------------+----------------------------------------------------|
| t   | =my/dashboards-open-this-day-history=          | All silos: chosen day of the current month, previous years only |
| j   | =my/dashboards-open-this-day-history-journals= | Same, journals only                                |

Only the day is asked for; the month stays the current one, so =t= with
=14= answers "what was written on the 14th of this month, in earlier
years". The exclusion of the current year is unchanged, which means a
note written on that day *this* year does not appear — the view is
about earlier years by definition.

**** This Day, Every Month

| Key | Function                                         | Description                                        |
|-----+--------------------------------------------------+----------------------------------------------------|
| m   | =my/dashboards-open-same-day-every-month=          | All silos: chosen day-of-month, any month, any year |
| M   | =my/dashboards-open-same-day-every-month-journals= | Same, journals only                                 |

Both prompt for the day of the month first, as the =t= / =j= pair
above does. =RET= on an empty prompt uses today's day, which is what
all four commands did before the prompt existed, so the habitual
keystroke sequence is unchanged.

Validation covers the range 1–31 and nothing else: non-numeric input
and a number outside that range raise a =user-error=, while a day that
some month does not have — 30 February, 31 April — is accepted and
simply matches nothing in those months. Treating it as an error would
mean deciding which years the day is being asked about, and the query
deliberately spans all of them.

Only the source of the day changed. Filtering, sorting and the layout
of =*Note History*= are the same code as before.

*Note:* "Every Month" excludes today's own date and does not require
notes to exist for a specific month — it collects whatever notes exist
for the chosen day across any month/year combination.

Functions: =my/dashboards--read-day-of-month=,
=my/dashboards--collect-this-day-history=,
=my/dashboards--collect-same-day-every-month= |
Module: =modules/21-dashboards.el=

Functions: =my/dashboards-open-this-day-history=,
=my/dashboards-open-this-day-history-journals=,
=my/dashboards-open-same-day-every-month=,
=my/dashboards-open-same-day-every-month-journals=
Module: =modules/21-dashboards.el=

** C-c n i — Insert
:PROPERTIES:
:CUSTOM_ID: menu-notes-insert
:END:

Insert links, timestamps, and other content at point. =t=, =i=, =I=,
=u= and =H= are appended dynamically by =20-transclusion.el=,
=31-org-images.el=, =32-web-links.el= and =33-denote-hubs.el=; =H= is
the one entry that writes into another file rather than at point.

Module: =modules/12-transient.el=

*** t — Transclusion submenu
:PROPERTIES:
:CUSTOM_ID: menu-notes-transclusion
:END:

Obsidian-style transclusion for Denote notes and arbitrary files.
Lets you choose a source, then a target inside that source, and inserts
both a live =#+transclude:= directive and an export-time =#+INCLUDE:= directive.

**** Why a pair, and why export needs care

=#+transclude:= works in the editor; =#+INCLUDE:= is what actually
reaches an export, because transcluded regions carry a read-only text
property that the Org exporter is documented as sensitive to (issue
#86 in nobiot/org-transclusion reports an export failing on it with no
file produced).

The pairing creates its own hazard: with a transclusion *expanded*,
the source text is already in the buffer, so =#+INCLUDE:= would pull it
in a second time and the export would contain it twice.
=my/transclusion--collapse-before-export= on
=org-export-before-processing-functions= therefore removes live
transclusions before every export. Removal is lossless — the
=#+transclude:= keyword remains and =A= re-expands afterwards.

**** Side effect worth knowing

Heading and paragraph targets need a stable anchor, so the wizard
writes =:CUSTOM_ID:= or =<<target>>= into the *source* file and saves
it. That means it modifies files you never opened. Anchoring by line
number or heading text instead would break on the first edit of the
source, so the write is the lesser cost — but commit before heavy
first use.

Full guide with workflow and examples:
=~/notes/docu/…org-transclusion-przyklady…org=

Module: =modules/20-transclusion.el=

**** a — Add transclusion (wizard)
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-transclude-insert
:END:

Interactive transclusion wizard.

Workflow:
1. Choose source type:
   - =Denote note=
   - =Any file on disk=
2. Choose target:
   - =Whole note=
   - =Paragraph=
   - specific heading
3. Insert both:
   - =#+transclude:= for live in-buffer rendering
   - =#+INCLUDE:= for export-time inclusion
4. Render transclusion immediately.

Heading mode:
If the chosen heading has no =CUSTOM_ID=, one is generated from the
heading title (spaces become hyphens, non-alphanumeric characters are
removed) and saved into the source file.

Paragraph mode:
Paragraphs are previewed one by one in a temporary preview window.
Use:
- =n= next
- =p= previous
- =RET= select
- =q= or =C-g= cancel

If the selected paragraph has no target anchor, the function appends a
generated =<<target>>= anchor to that paragraph and saves the source file.

*Warning:* this command may modify and save the source file on disk
(adding =CUSTOM_ID= or =<<target>>=). Commit before heavy first use.

Function: =my/denote-transclude-insert= | Module: =modules/20-transclusion.el=

**** A — Add all in buffer
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-add-all
:END:

Activates every =#+transclude:= keyword in the current buffer at once.

Function: =org-transclusion-add-all= (org-transclusion package)

**** g — Refresh at point
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-refresh
:END:

Re-reads the source and updates the transcluded copy at point after
the source has changed.

Function: =org-transclusion-refresh= (org-transclusion package)

**** r — Remove at point
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-remove
:END:

Removes the transcluded content at point, restoring the plain
=#+transclude:= keyword line. Non-destructive to the source file.

Function: =org-transclusion-remove= (org-transclusion package)

**** T — Toggle mode
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-mode
:END:

Toggles =org-transclusion-mode= for the current buffer. With
=org-transclusion-add-all-on-activate= set to =nil=, activation does
not automatically expand all transclusions.

Function: =org-transclusion-mode= (org-transclusion package)

**** o — Open source at point
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-open-source
:END:

Jumps from the transcluded region to its source location.

Function: =org-transclusion-open-source= (org-transclusion package)

**** O — Move to source
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-move-to-source
:END:

Moves point from the transcluded copy to the corresponding place in the
source buffer.

Function: =org-transclusion-move-to-source= (org-transclusion package)

**** e — Live-sync edit
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-live-sync-start
:END:

Starts editing the transcluded content in live-sync mode so changes are
propagated back to the source.

Function: =org-transclusion-live-sync-start= (org-transclusion package)

**** E — Exit live-sync
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-live-sync-exit
:END:

Exits live-sync editing mode for the current transclusion.

Function: =org-transclusion-live-sync-exit= (org-transclusion package)

**** P — Promote subtree
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-promote-subtree
:END:

Promotes the transcluded Org subtree at point by one level.

Function: =org-transclusion-promote-subtree= (org-transclusion package)

**** D — Demote subtree
:PROPERTIES:
:CUSTOM_ID: fn-org-transclusion-demote-subtree
:END:

Demotes the transcluded Org subtree at point by one level.

Function: =org-transclusion-demote-subtree= (org-transclusion package)*** l — Insert link
:PROPERTIES:
:CUSTOM_ID: fn-denote-link
:END:

Inserts a =[[denote:ID][Title]]= link to an existing note, with
completion over all notes.

Function: =denote-link= (Denote package)

*** h — Time (HH:MM)
:PROPERTIES:
:CUSTOM_ID: fn-insert-current-time
:END:

Inserts current time in =HH:MM= format at point.

Function: =insert-current-time= | Module: =modules/05-notes.el=

*** d — Date (YYYY-MM-DD)
:PROPERTIES:
:CUSTOM_ID: fn-insert-current-date
:END:

Inserts current date in =YYYY-MM-DD= format at point.

Function: =insert-current-date= | Module: =modules/05-notes.el=

*** i — Image from disk
:PROPERTIES:
:CUSTOM_ID: fn-my-org-image-insert
:END:

Copies an image from anywhere on disk into =~/notes/attachments/=,
shrinks it, and links it at point. Two prompts: the file, then a name
with the note's =#+title:= offered as the default (=RET= accepts it).
Set =my/org-image-prompt-for-name= to nil to skip the second prompt.

With a prefix argument (=C-u C-c n i i=) the image is stored exactly as
it is, at full resolution and without recompression.

The echo area reports source size, stored size and what was done, e.g.
=…-0.png 4.2M → 148k (rescaled, 256-colour palette)=.

Function: =my/org-image-insert= | Module: =modules/31-org-images.el=

**** How the file is named

: IDENTIFIER--TITLE-SLUG-N.EXT
: 20241225T000000--25-12-2024-środa-0.png

The identifier belongs to the *note the image is inserted into*, the
slug comes from its title, and =N= counts from 0 within that note. So
every attachment of one note sorts next to its siblings, and a file
whose identifier matches no note is an orphan by inspection. A note
without a Denote identifier falls back to =#+identifier:= and then to
its file name, which is what makes the command work in =captures.org=.

The number ignores the extension, because one note can end up with a
PNG and a JPEG and they must not both claim =-0=.

Reusing the note's identifier means two files under =~/notes/= carry the
same one. The attachment folder is therefore added to
=denote-excluded-directories-regexp= at load time — extending the value
=25-inbox-review.el= sets for the inbox, never replacing it — so Denote
prompts, backlinks and keyword completion never see it.
=26-maintenance.el= and =27-denote-identifiers.el= read =.org= files
only, so images were already invisible to them.

The link is relative (=[[file:../attachments/…]]=), which survives a
home directory that differs between machines. =my/org-image-link-style=
set to =absolute= changes that.

**** How the file is shrunk

Rescaling alone barely helps, because PNG is lossless: a 4 MB screenshot
at 1600 px is still nearly 4 MB. Size falls only when fidelity is
traded, so each step below runs only if the one before left the file
over =my/org-image-max-bytes=:

1. rescale to =my/org-image-max-pixels=, never enlarging;
2. under budget — stop, and the file is still pixel-exact;
3. few colours in the source (screenshot, diagram, chart) — quantise to
   256 colours, staying PNG and keeping transparency;
4. otherwise, or when step 3 was not enough — JPEG at
   =my/org-image-jpeg-quality=.

Measured on a 2559x1639 terminal screenshot: 551 kB source, 489 kB after
rescaling alone, 133 kB after quantising, 146 kB as JPEG.

Screenshots and photographs are told apart by counting distinct colours
in a small sample of the source, against
=my/org-image-palette-max-colors= (4096). Lower it if screenshots come
out looking like JPEG; raise it if photographs come out banded.

| Setting                        | Meaning                          | Default |
|--------------------------------+----------------------------------+---------|
| =my/org-image-max-pixels=      | longer edge of the stored file   |    1600 |
| =my/org-image-max-bytes=       | size budget for the stored file  |  300 kB |
| =my/org-image-palette-max-colors= | screenshot / photograph split |    4096 |
| =my/org-image-jpeg-quality=    | quality when JPEG is chosen      |      85 |
| =org-image-actual-width=       | width of the *preview* on screen | =(1100)= |

The last one lives in =modules/11-org-appearance.el= and is a display
setting, not a storage setting. Displaying more pixels than the stored
file has only upscales a bitmap, so raise it and
=my/org-image-max-pixels= together or neither. A single image can
override the width with =#+ATTR_ORG: :width 900= above its link.

All of this needs ImageMagick (=magick=, or =convert= on version 6) on
=PATH= — on NixOS, =imagemagick= in the system packages. Without it the
file is copied unchanged and the message says so; nothing fails. SVG and
GIF are always copied unchanged.

**** When an image is stored unchanged anyway

Both executables are tried in turn, and a failing run leaves its command
line, exit status and output in =*org-image-log*=; the echo area names
that buffer. A file that arrives at its original size with
=conversion failed= in the message means ImageMagick ran and refused the
job, not that it is missing.

=M-x my/org-image-diagnose= asks for an image and collects the same
information on demand: which executables are on =PATH=, their versions,
the exact command line insertion would run, its exit status and its
output. Nothing is stored and the file is not touched.

The usual causes are a restrictive =policy.xml= (=magick -list policy=
in a terminal reports it) and a missing delegate for the source format
(the =Delegates= line of =magick -version=).

**** Clicking a picture

Inline previews carry a keymap:

- =mouse-1=, =RET= — open the image. In an Emacs =image-mode= buffer by
  default; set =my/org-image-click-action= to =external= for the desktop
  image viewer, or to =nil= to turn the whole thing off.
- =x= — open in the desktop viewer regardless of that setting.
- =i +=, =i -=, =i o= — Emacs' own image commands still work; the keymap
  inherits =image-map=.

A click landing on anything other than an image just moves point.

Growing the preview in place is deliberately not what this does: it
rescales a bitmap inside the text buffer, reflows everything below it,
and loses the size at the next image refresh.

*** I — Attachments folder
:PROPERTIES:
:CUSTOM_ID: fn-my-org-image-attachments-dired
:END:

Opens =~/notes/attachments/= in Dired, creating it if it is not there
yet. Useful for spotting orphans: the identifier prefix of every file
names the note it was inserted into.

Function: =my/org-image-attachments-dired= | Module: =modules/31-org-images.el=

**** M-x my/org-image-recompress-attachments
:PROPERTIES:
:CUSTOM_ID: fn-my-org-image-recompress-attachments
:END:

One-off cleanup for files stored before the size budget existed, or
before ImageMagick was installed. Rescales and recompresses every
attachment over =my/org-image-max-bytes= in place, keeping it only when
the result is smaller, and asks for confirmation with the count and the
total size first.

Names and extensions are preserved, so no link has to change — which is
also the limit: a photograph stored as PNG is quantised rather than
converted to JPEG, and may band. Deleting that file and inserting it
again gives a better result, because the insertion path can choose the
format.

Function: =my/org-image-recompress-attachments= | Module: =modules/31-org-images.el=
*** u — Web link (auto title)
:PROPERTIES:
:CUSTOM_ID: fn-my-insert-web-link
:END:

Fetches the page title for a URL and inserts an Org link
[[URL][Title]] at point. Prompts for the URL; if the clipboard
already contains one, org-web-tools offers it as default.

Function: =my/insert-web-link= | Module: =modules/32-web-links.el=

*** H — Add to HUB
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-add-to-hub
:END:

Appends a link to the *current* note at the end of a hub note. The only
entry in this menu that writes into a file other than the current
buffer, and the only one that inserts nowhere near point.

A hub is an ordinary Denote note carrying the =hub= keyword in the
=__keyword= component of its file name. Its body is a plain Org list:

: - [[denote:20260202T110000][1a2 Zwykła notatka]] — why it belongs here

The prompt offers =[Nowy HUB]= first, then the hubs that already link
to this note with the description they currently give it, then the
rest:

: [Nowy HUB]
: HUB A (20260101T100000)    already listed: bo pasuje do tematu
: HUB B (20260101T110000)    already listed
: HUB C (20260101T120000)

So the answer to "is this note already filed somewhere, and under what
note to self" arrives before the decision rather than after it.

Creating a hub comes first because the list only grows: scrolling past
a hundred hubs to reach it would be the one operation that gets harder
the longer the system is used. It is also the only candidate starting
with a bracket, so typing =[= selects it.

Three outcomes:

- *A hub that does not list the note yet* — one prompt for the
  description, and the entry is inserted among the existing ones in
  chronological order, the hub being sorted first if it was not already.
- *A hub that already lists it* — the same prompt, pre-filled with the
  existing description, and the entry is rewritten where it stands.
  This is why the command cannot produce a duplicate entry: adding a
  note twice is not one of the outcomes. A hub that already contains a
  duplicate (from before this behaviour existed) is refused with a
  count, rather than having one of the two entries picked arbitrarily.
- *=[Nowy HUB]=* — asks for the new hub's title, a one-line description
  of the hub itself, and then the entry description. The hub
  description becomes the first line of the new note's body, wrapped in
  =*= so it renders bold, and the entry goes one blank line below it.

Function: =my/denote-add-to-hub= | Module: =modules/33-denote-hubs.el=

**** Entries stay in chronological order
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-hub-sort-entries
:END:

A new entry goes before the first existing entry with a newer
identifier, so a hub reads oldest-first with the newest at the bottom.

*A hub whose entries are out of order is sorted first, automatically,
as part of adding.* That is not a convenience — "before the first entry
that is newer" only means something in a sorted list. In a jumbled one
it is whichever newer entry happens to come first in the file, which is
no position at all. A hub already in order is not rewritten; the check
costs nothing, since the identifiers come from the scan that has to
happen anyway.

So, adding a link reports one of three things:

| Message                       | What happened                                       |
|-------------------------------+-----------------------------------------------------|
| =Added to hub: X=             | The hub was in order; the entry went in place       |
| =… entries were out of order and have been sorted= | The hub was sorted first, then the entry went in place |
| =Added at the end of X: prose between its entries…= | The hub could not be sorted; the entry went to the end |

The third case *never refuses*. Adding a link is not the moment to make
someone reformat a hub, and the end is at least a predictable place.

This costs no file access beyond the hub. The identifier is already
written into the link — =denote:20230825T090000= — so ordering a hub
means comparing strings that are already in the buffer. *None of the
linked notes is opened.* The hub itself is visited through
=my/denote-hub--with-hub=, which kills the buffer again unless it was
already open, so even sorting every hub in the collection holds one
buffer at a time.

Identifiers are fixed-width =YYYYMMDDTHHMMSS=, so lexicographic order
is chronological order and nothing has to parse a date.

Insertion *rewrites nothing*. The entry is inserted at one position and
every other line is left byte-for-byte as it was, which is what makes
this safe on a hub with hundreds of entries — there is no pass that
could reorder or drop anything.

***** M-x my/denote-hub-sort-entries

Rarely needed, since adding a link sorts the hub by itself. It exists
for two things: sorting a hub without adding anything to it, and finding
out *why* the automatic sort declined. Sorts the current buffer's hub,
or prompts for one when called elsewhere. No keybinding.

Where adding a link merely appends, this command refuses and names the
line: if anything but blank lines separates two entries, sorting would
sweep that paragraph along and land it somewhere else, and nothing in
the file says which entries it was commenting on. A hub already in order
is left untouched and says so rather than rewriting itself into an
identical file.

Measured on a generated 500-entry hub, half of them multi-paragraph:
sort 0.42 s, subsequent insert 0.014 s, all descriptions and paragraph
breaks intact, no buffers left behind.

***** Scope: the last section

Both the insertion and the sort consider the entries below the *last Org
heading*, or all of them in a hub with no headings. That is the same
place appending always wrote to, so a hub split into sections keeps its
sections and only the position within the last one changes.

**** Multi-line entry descriptions

=S-RET= at an entry description prompt breaks the line; =RET= still
accepts. =M-RET= does the same, and =C-q C-j= works with no binding at
all — which matters on a terminal, where =S-RET= and =RET= arrive as
the same key. The minibuffer grows to fit.

Continuation lines are written *indented by two spaces*:

#+begin_src org
- [[denote:20230825T090000][2023-08-25 Journal]] — „Ponieważ życie nie ma
  żadnego sensu trzeba sobie wybrać jakiś."

  „Im więcej dowiaduję się na jakiś temat, tym mniej chętnie przyjmuję
  skrajne stanowiska."
#+end_src

The indentation is structural, not cosmetic. Org ends a plain list item
at the first non-blank line in column zero, so an unindented
continuation line after a blank line is a *separate paragraph that
merely looks attached*: it exports outside the list, folds separately,
and gives this code no way to tell where one entry stops and the next
piece of prose begins. Indented, the extent of an entry is unambiguous,
which is what makes editing one in place safe.

The indentation is added on the way in and removed on the way out, so
the prompt shows what was typed, not what is in the file. A description
round-trips unchanged through write and re-edit.

The hub's own description stays one line by design: it is wrapped in
Org emphasis markers, and those do not carry across a blank line.

An entry ends at the first non-blank line in column zero, or at a second
consecutive blank line — Org's own rule for terminating a list. Trailing
blank lines are excluded, so replacing an entry does not eat the space
below it or swallow the entry that follows.

Entry descriptions are inserted verbatim. Emphasis there is the author's
business; only the hub description is marked up automatically, because
it is a heading for everything under it and reads as one. A hub
description already wrapped in =*= is left alone rather than doubled.

**** M-x my/denote-hub-list-for-note
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-hub-list-for-note
:END:

Reports in the echo area which hubs list the current note and with what
description, one line each. The same information the prompt above shows
beside its candidates; this command exists for asking the question on
its own, without intending to add anything. No keybinding.

**** Membership is derived, not stored

A note is in a hub because the hub links to it. That link is the only
record, and there is deliberately no =#+hubs:= field in the note.

A field would be a second copy of one fact, and the two would drift the
first time a line was deleted from a hub by hand — the note would go on
claiming membership that no longer exists. It is the same reason Denote
itself stores no backlinks in front matter and greps for them instead,
and the same reason the =hub= keyword lives in the file name: one
mechanism, nothing to keep in step.

The cost is that the question has to be answered by reading, and
=my/denote-hub-membership= pays it by reading only the hub notes — tens
of files, not the collection. =denote-get-backlinks= would answer the
membership question too, but it greps the whole notes tree and returns
only the file name, and the description lives in the line.

A live buffer visiting a hub wins over the copy on disk, so a hub being
edited reports what it currently says rather than what was last saved.

**** Parsing an existing entry

The description is taken as everything after the closing brackets of
the link, with a leading dash of any kind removed, plus every
continuation line de-indented. Deliberately liberal: what is in the file
is whatever was written there, possibly edited by hand since, and an
entry that does not match the exact output format is still worth reading
rather than reporting as absent.

Where a description has to fit on one line — a completion annotation,
the echo-area report of =my/denote-hub-list-for-note= — only its first
line is shown, followed by an ellipsis when more follows.

**** What the link description is

The =#+title:= value of the source note, verbatim. A note whose title
already begins with its Folgezettel signature contributes that
signature; a note without one gets nothing added. Denote's own
=denote-get-link-description= is deliberately *not* used here, because
it composes signature and title itself and would double the signature
on notes that carry it in the title.

**** Why hubs are found by keyword

=my/denote-hub-files= filters =denote-directory-files= through
=denote-extract-keywords-from-path=. Both read file names only, so
deciding whether a note is a hub costs no file access at all — the same
reasoning as =my/dashboards--journal-p=. A content scan for something
like a =#+hub:= keyword would open every note in the collection on
every invocation.

Because the keyword lives in the file name, =denote-rename-file= is
what promotes an existing note to a hub or demotes it back, and no
separate bookkeeping exists to fall out of step.

**** How the prompt is ordered and annotated

Two pieces of completion metadata. =identity= as the
=display-sort-function= keeps the order the command builds — =[Nowy
HUB]=, then already listed, then the rest — which the completion
frontend would otherwise re-sort alphabetically. The
=annotation-function= reads a hash of candidate to text and returns nil
for the candidates that have none, so unlisted hubs stay unadorned.

**** Buffer handling

A hub already open in a buffer is written to in that buffer and left
open. A hub that was not open is visited with =find-file-noselect=,
written, saved and killed again, so working through hubs does not
accumulate buffers.

The cleanup is in an =unwind-protect=, so an operation that signals —
=my/denote-hub-sort-entries= refusing a hub with prose between its
entries — does not leak the buffer it opened. The one case where a
buffer is deliberately left behind is an operation that modified it and
then failed: killing it would discard the half-finished edit, and
leaving it visible is what allows it to be inspected.

The separator is normalised rather than assumed: trailing whitespace at
the end of the hub is removed before the entry is inserted, so the
result is exactly one blank line whatever the file happened to end
with.

**** Configuration

| Variable                  | Default            | Effect                                |
|---------------------------+--------------------+---------------------------------------|
| =my/denote-hub-keyword=   | ="hub"=            | Keyword identifying a hub note        |
| =my/denote-hub-directory= | =my-notes-pks=     | Silo new hubs are created in          |
| =my/denote-hub-new-label= | ="[Nowy HUB]"=     | Label of the create-a-new-hub choice  |

New hubs go to the pks silo without asking, since the specified
creation flow prompts for a title only. Set =my/denote-hub-directory=
to =my-notes-docu= or =my-notes-journal= to change that.

**** Not the same thing as a writing-project hub

=28-writing-projects.el= also has hubs: scaffolding files under
=~/projects/= that hold tasks, clock entries and materials sections for
one deadline. Those are not Denote notes and are not found by keyword.
The two live in separate namespaces — =my/writing--hub-*= there,
=my/denote-hub--*= here — because a shared prefix is exactly how two
modules end up redefining each other's helpers (see the
=my/denote--silo-files= collision in the change log).

** C-c n d — Document / File management
:PROPERTIES:
:CUSTOM_ID: menu-notes-doc
:END:

Rename, retag, move between silos, and delete notes.
Module: =modules/12-transient.el=

*** r — Rename file
:PROPERTIES:
:CUSTOM_ID: fn-denote-rename-file
:END:

Renames the current Denote file, updating front matter to match
(title, keywords, date in filename and =#+= headers).

Function: =denote-rename-file= (Denote package)

*** k — Add keywords
:PROPERTIES:
:CUSTOM_ID: fn-denote-rename-file-keywords
:END:

Adds or changes keywords in the current Denote filename and front
matter. Thin wrapper around Denote's =denote-rename-file-keywords=,
adding nothing but the prompt behaviour described in
[[#keyword-prompts][Keyword Prompts]]; which file is acted on and how front matter is
rewritten stays Denote's business.

Function: =my/denote-keywords-edit= | Module: =modules/05-notes.el=

*** c — Change silo
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-move-to-silo
:END:

Moves the current Denote note to another silo (=journal=, =pks=,
=docu=). Prompts for the destination, excluding the silo the note is
already in.

**** Conflict handling

The destination is scanned for notes whose =#+title:= matches the one
being moved (comparison ignores case and surrounding whitespace).

| Situation in target       | What happens                                     |
|---------------------------+--------------------------------------------------|
| No matching title         | Move proceeds immediately                        |
| Same title, different =#+identifier:= | Prompt: keep both / overwrite / cancel |
| Same title AND identifier | Prompt for a new title, or cancel                |

"Keep both" is the default and the normal Denote situation: two notes
may legitimately share a title as long as their identifiers differ,
because the identifier is what makes the file name unique.

"Overwrite" deletes the target note (via =git rm= when tracked) after a
=yes-or-no-p= confirmation. When several notes in the target share the
title, it asks which one to replace.

A new title is applied by rewriting the =#+title:= line and then
calling =denote-rename-file-using-front-matter=, so Denote derives the
new file name itself rather than this code reimplementing sluggifying.

***** Caveat: retitling does not fix a duplicate identifier

If two notes share an identifier, giving one of them a new title
unblocks the move but leaves the real problem in place: =denote:= links
resolve by identifier, so links pointing at that identifier become
ambiguous. Treat the retitling path as a way to get unstuck, then
resolve the duplicate separately (decide which note is canonical and
delete or re-create the other).

**** Mechanics

- The move uses =git mv= when the file is tracked, so history follows
  the file instead of showing an unrelated delete plus add. Falls back
  to =rename-file= if git refuses or the file is untracked.
- Unsaved changes must be saved first (the command offers to do it).
- The visiting buffer is repointed at the new path with
  =set-visited-file-name=, so no reopening is needed.
- Silo membership is defined by =my/denote-silo-alist=; add an entry
  there when a new silo is added to =00-core.el=.
- Silo scanning is non-recursive, matching the flat layout of the
  silos in this configuration.

*Links survive the move.* Denote links resolve by identifier, not by
path or file name, so moving a note between silos never breaks incoming
or outgoing links.

Functions: =my/denote-move-to-silo=, =my/denote--file-title=,
=my/denote--silo-note-files=, =my/denote--current-silo=,
=my/denote--titles-equal-p=, =my/denote--relocate=, =my/denote--remove=
| Variable: =my/denote-silo-alist= | Module: =modules/05-notes.el=

Identifiers are read with =my/denote--file-identifier=, which lives in
=modules/27-denote-identifiers.el= — see [[#symbol-collisions][One symbol, one owner]].

*** d — Delete note
:PROPERTIES:
:CUSTOM_ID: fn-my-denote-delete-note
:END:

Deletes current note file and kills its buffer. If tracked by Git, uses
=git rm -f=; otherwise plain =delete-file=. Prompts for confirmation.

*Warning:* =git rm -f= bypasses staging — deletion is immediate in the
working tree. Commit or stash important changes first.

Function: =my/denote-delete-note= | Module: =modules/05-notes.el=

** C-c n x — Export
:PROPERTIES:
:CUSTOM_ID: menu-notes-export
:END:

Export notes to PDF using LuaLaTeX. Output mirrors silo structure in
=~/notes/pdf/=. Single-file export also available as global =C-c p=.
Module: =modules/16-org-export.el=

*** p — Export to PDF
:PROPERTIES:
:CUSTOM_ID: fn-my-org-export-to-pdf
:END:

Exports current buffer to PDF. Language auto-detected from
=#+filetags:=: =:en:= → English, default → Polish. Journal files
(=:journal:=) use Playpen Sans Hebrew font. Filename from =#+title:=.
Prompts on overwrite: =o= overwrite, =r= rename with index, =q= cancel.
Build files isolated in =/tmp=, deleted after export.

Function: =my/org-export-to-pdf= | Module: =modules/16-org-export.el=

*** P — Batch PDF — ANY keyword
:PROPERTIES:
:CUSTOM_ID: fn-my-org-export-pdf-by-keyword
:END:

Exports all notes whose filenames match *any* of the given keywords.
Keyword input via =completing-read-multiple= with TAB completion over
all Denote tags in use. Results shown in =*PDF Export Results*= buffer.

Function: =my/org-export-pdf-by-keyword= | Module: =modules/16-org-export.el=

*** Q — Batch PDF — ALL keywords
:PROPERTIES:
:CUSTOM_ID: fn-my-org-export-pdf-by-all-keywords
:END:

Exports all notes whose filenames match *all* given keywords
simultaneously. More selective than =P=; useful for scoped sets
(e.g. =esej= + =project=).

Function: =my/org-export-pdf-by-all-keywords= | Module: =modules/16-org-export.el=

** C-c n v — View
:PROPERTIES:
:CUSTOM_ID: menu-notes-view
:END:

Toggle visual appearance settings for comfortable writing.

Three entries are not defined in =12-transient.el=: they are appended at
load time through =my/transient-append=, so removing the module that
contributes one leaves the rest of the submenu intact.

| Key | Contributed by |
|-----+----------------|
| =T= Link tooltips | =30-link-tooltips.el= |
| =t= Theme light/dark | =34-appearance.el= |
| =p= Padding | =34-appearance.el= |

=t= and =T= are different entries. =my/transient-append= skips an
already-bound key *silently*, which is right for re-evaluating a module
during development and wrong for a genuine collision: the entry simply
does not appear and nothing is said about it. Before appending here,
check the key against this table as well as against
=12-transient.el= itself.

Module: =modules/12-transient.el= / =modules/13-centered-writing.el=

*** c — Center text
:PROPERTIES:
:CUSTOM_ID: fn-my-toggle-visual-fill-column-center
:END:

Toggles centered text layout (=visual-fill-column= + centering).

Function: =my/toggle-visual-fill-column-center= | Module: =modules/13-centered-writing.el=

*** w — Writing mode
:PROPERTIES:
:CUSTOM_ID: fn-my-toggle-writeroom
:END:

Toggles full-screen distraction-free writing mode (=writeroom-mode=).

Function: =my/toggle-writeroom= | Module: =modules/13-centered-writing.el=

*** i — Indent headings
:PROPERTIES:
:CUSTOM_ID: fn-my-toggle-org-indent
:END:

Toggles =org-indent-mode=, visually indenting body text under headings.

Function: =my/toggle-org-indent= | Module: =modules/11-org-appearance.el=

*** e — Emphasis markers
:PROPERTIES:
:CUSTOM_ID: fn-my-toggle-emphasis-markers
:END:

Toggles visibility of Org emphasis markers (=*bold*=, =/italic/=, etc.).

Function: =my/toggle-emphasis-markers= | Module: =modules/11-org-appearance.el=

*** T — Link tooltips
:PROPERTIES:
:CUSTOM_ID: fn-my-link-tooltip-set-style
:END:

Chooses what the mouse tooltip over a =denote:= link shows. Prompts
for =identifier=, =path= or =default=. See [[#link-tooltips][Link Tooltips]] for why the
default is not =path=.

Function: =my/link-tooltip-set-style= | Module: =modules/30-link-tooltips.el=

*** t — Theme light/dark
:PROPERTIES:
:CUSTOM_ID: fn-modus-themes-toggle
:END:

Switches between =modus-operandi-tinted= (light) and
=modus-vivendi-tinted= (dark). Also =<f5>= and =C-c u t=. See
[[#theme][Theme]] for what survives the switch and how.

Function: =modus-themes-toggle= | Module: =modules/09-theme.el=

*** p — Padding
:PROPERTIES:
:CUSTOM_ID: fn-my-toggle-spacious-padding
:END:

Turns =spacious-padding-mode= on or off: frame border, window dividers,
fringe margins and the hairline mode line. Worth turning off when screen
space is tight. Also =C-c u p=. See [[#appearance][Appearance layer]].

Function: =my/toggle-spacious-padding= | Module: =modules/34-appearance.el=

** C-c n t — Tools
:PROPERTIES:
:CUSTOM_ID: menu-notes-tools
:END:

External tool integrations and spelling. New external integrations
should be added here rather than at top level.
Module: =modules/12-transient.el=

*** z — Zotero / Bib
:PROPERTIES:
:CUSTOM_ID: menu-zotero
:END:

Opens bibliography/Zotero submenu. Also available directly as =C-c x=.
See [[#menu-zotero-detail][Zotero menu detail]] below.

Function: =my/zotero-menu= | Module: =modules/18-zotero-transient.el=

*** i — Inbox (migrated notes)
:PROPERTIES:
:CUSTOM_ID: menu-inbox-entry
:END:

Opens the review queue for notes migrated from Obsidian.
See [[#menu-inbox][Inbox review detail]] below.

Function: =my/inbox-menu= | Module: =modules/25-inbox-review.el=

*** Spelling commands
:PROPERTIES:
:CUSTOM_ID: menu-spelling
:END:

| Key | Function                        | Description                      |
|-----+----------------------------------+-----------------------------------|
| s   | =my/spell-correct-previous=     | Correct previous misspelled word |
| a   | =my/spell-add-previous-to-dict= | Add previous word to dictionary  |
| S   | =my/spell-check-visible=        | Check visible portion of buffer  |
| b   | =my/spell-check-buffer-full=    | Check entire buffer              |
| T   | =my/toggle-flyspell=            | Toggle flyspell mode             |

Module: =modules/03-spelling.el=

** C-c n l — Philosophy
:PROPERTIES:
:CUSTOM_ID: menu-notes-philosophy
:END:

Dynamically appended by =19-philosophy-notes.el= after =12-transient.el=
loads (via =transient-append-suffix=). Five note types, each saved in
=pks= silo with a type keyword plus optional epoch/tradition keyword.

Module: =modules/19-philosophy-notes.el=

| Key | Note type  | Denote keyword | Scaffold headings                                            |
|-----+------------+-----------------+---------------------------------------------------------------|
| l   | Literature | =literatura=    | Streszczenie, Linki                                            |
| p   | Concept    | =pojęcie=       | Definicja, Skąd się wzięło, Napięcia i krytyka, Powiązania    |
| m   | Thinker    | =myśliciel=     | Centralny projekt, Problem, Kluczowe ruchy, Komu odpowiada    |
| b   | Problem    | =problem=       | Geneza, Proponowane rozwiązania, Krytyka, Pytania otwarte     |
| i   | Map / MOC  | =mapa=          | Wielkie pytania, Postacie, Pojęcia i problemy, Linki          |

Epoch/tradition keywords offered at creation: =starożytność=,
=średniowiecze=, =nowożytność=, =współczesność=, =analityczna=,
=kontynentalna= (open completion list, not a fixed set).

** C-c n h — Function Help
:PROPERTIES:
:CUSTOM_ID: fn-open-function-helper
:END:

Opens this file in a new tab. Uses =tab-bar-new-tab= + =find-file= +
=org-overview= (collapses all headings on open).

Function: =my/open-function-helper= | Module: =modules/12-transient.el=

* Fixed Tabs (routing commands to named tabs)
:PROPERTIES:
:CUSTOM_ID: fixed-tabs
:END:

Some activities recur often enough to deserve a permanent home. The
routing layer makes the command behind such an activity always run in
a tab of its own, so that invoking the command *is* the gesture of
going to that tab.

** The convention it maintains

An Emacs tab is a named window configuration, and any buffer can be
displayed in any tab — nothing in Emacs ties a tab to a particular
file, the way a browser tab is tied to a page. The routing does not
change that; it makes the browser-like convention hold automatically
rather than by hand.

** Registering a command

Add a =(COMMAND . "Tab name")= pair to =my/fixed-tab-commands=, then
run =my/fixed-tab-refresh-advice= (or restart). Currently registered:

| Command                  | Tab       |
|--------------------------+-----------|
| =my/denote-journal=      | Journal   |
| =my/denote-journal-date= | Journal   |

Both journal commands share one tab deliberately: the tab is the place
where journalling happens, not one entry point into it.

Commands that build their own layout — =my/open-notes-dashboard=,
=my/dashboards--goto-tab= — call =my/fixed-tab-goto= directly instead
and are *not* registered, since routing them here as well would switch
tabs twice.

** Why advice rather than wrapper commands

Advice covers every route into a command at once: the keybinding,
=M-x=, a transient menu entry, and =emacsclient -n -e '(my/denote-journal)'=
from outside Emacs. Wrapper commands would only cover the paths
rebound to them.

The advice carries the name =my/fixed-tab=, so re-evaluating the
module replaces it instead of stacking a second copy.

** Why not =display-buffer-alist=

The declarative route would be =display-buffer-alist= with the
=display-buffer-in-tab= action, and for plain buffer display that is
the better tool. It does not fit here because these commands do more
than display a buffer: =my/denote-journal= finds a file, moves point
to the end and inserts a timestamped heading. Routing only the
*display* would leave the editing happening wherever point already
was. Switching tab first and running the whole command there also
avoids having to set =switch-to-buffer-obey-display-actions= globally,
which would change how every =find-file= in the configuration behaves.

** =my/fixed-tab-goto=

Switches to a named tab, creating it immediately to the right of the
current one when absent, and returns non-nil when it had to create
it. Binds =tab-bar-new-tab-to= explicitly rather than relying on its
global value, and skips the switch when the target tab is already
current (which would otherwise push a redundant entry onto the tab
switching history).

Functions: =my/fixed-tab-goto= (=01-ui.el=),
=my/fixed-tab-refresh-advice=, =my/fixed-tab--install= |
Variable: =my/fixed-tab-commands= | Module: =modules/23-fixed-tabs.el=

** Detaching to a separate frame (C-c n v f / F)
:PROPERTIES:
:CUSTOM_ID: detach-to-frame
:END:

The counterpart to fixed tabs. A tab is a destination you return to,
and it is exclusive: while you look at it, you are not looking at
anything else. A frame is a separate operating-system window, so it can
sit on another monitor and be read *alongside* the work.

That difference decides which to use. Journal and Dashboard are places
you go to, so they get tabs. A keybinding cheat sheet, or a list of old
entries being skimmed, is something wanted *beside* the work, so it
wants a frame.

| Key       | Command                      | Effect                                  |
|-----------+------------------------------+-----------------------------------------|
| =C-c n v f= | =my/detach-buffer-to-frame=  | Current buffer into a new frame         |
| =C-c n v F= | =my/detach-tab-to-frame=     | Current tab, layout intact, into a frame |

=f= moves rather than duplicates: the window that showed the buffer is
closed afterwards, unless it was the only window in the tab (deleting
that would take the tab with it).

=F= wraps =tab-bar-detach-tab= (Emacs 28+) and preserves splits, so it
is the right choice for a tab whose value lies in its layout — the
History tab with its list-and-preview panes, for instance. It refuses
when only one tab exists, since detaching it would empty the frame.

Frame size comes from =my/detached-frame-parameters=, deliberately
modest because these frames are for reading beside the main one.

*Monitor placement is not settable from Emacs* — that belongs to the
window manager. These commands create the frame; where it appears is
up to the WM. Moving it once by hand is usually enough, since most
window managers remember placement per window.

* Zettelkasten / Folgezettel (C-c n z)
:PROPERTIES:
:CUSTOM_ID: menu-zettelkasten
:END:

Luhmann-style sequence notes built on the official =denote-sequence=
package (GNU ELPA, stable 0.3.0 released 2026-05-20). Adds hierarchical
relationships on top of ordinary Denote notes by filling in the
optional =SIGNATURE= file name component.

** Scope and design decisions
:PROPERTIES:
:CUSTOM_ID: zettelkasten-scope
:END:

- *pks silo only.* Every command in this menu is wrapped by
  =my/zettel--in-pks=, a macro binding =denote-directory= to
  =my-notes-pks=. Journal and docu notes therefore never receive a
  signature, and sequence lookups never offer them. This mirrors the
  silo-switching pattern of =my/denote-base= in =05-notes.el=. The
  rationale is that a journal is time-structured while a Zettelkasten
  is idea-structured; mixing them makes both harder to navigate.
- *Alphanumeric scheme* (=denote-sequence-scheme= is ='alphanumeric=):
  signatures read =1=, =1a=, =1a1=, =1b=, =2=. The Denote default is
  ='numeric= (=1=, =1=1=, =1=1=2=), which is easier to reason about for
  large numbers but far longer inside file names. Not an irreversible
  choice: =denote-sequence-convert= converts a whole collection between
  schemes in one command (it does *not* reparent or check for
  duplicates, it only rewrites the notation).
- *Signatures are optional.* Plain Denote notes and sequence notes
  coexist in the same silo. A signature is added only when a thought
  explicitly continues or branches from another one. Per the
  denote-sequence manual, the extension "is not necessary for such a
  workflow" — it only streamlines signature bookkeeping.

** Reading a signature
:PROPERTIES:
:CUSTOM_ID: zettelkasten-signature-semantics
:END:

In the alphanumeric scheme, depth is communicated by alternating
between digits and letters, so =1a2= is three levels deep.

| Signature | Relationship                                          |
|-----------+-------------------------------------------------------|
| =1=       | A top-level thread, unrelated to =2=                  |
| =1a=      | First child of =1= — elaborates one concept from it   |
| =1a1=     | First child of =1a= — drills deeper still             |
| =1b=      | Second child of =1= — a different branch off =1=      |
| =2=       | A separate top-level thread                           |

Branch (create a *child*) to develop one specific concept out of a
note without interrupting its main line. Continue (create a *sibling*)
when the train of thought runs on at the same level. Numbering is
handled automatically: creating a child of =1a= when =1a1= exists
yields =1a2=.

** Commands
:PROPERTIES:
:CUSTOM_ID: zettelkasten-commands
:END:

| Key | Command                       | Wraps                                    |
|-----+-------------------------------+------------------------------------------|
| n   | =my/zettel-new-parent=        | =denote-sequence-new-parent=             |
| c   | =my/zettel-new-child-of-current= | =denote-sequence-new-child-of-current= |
| C   | =my/zettel-new-child=         | =denote-sequence-new-child=              |
| s   | =my/zettel-new-sibling-of-current= | =denote-sequence-new-sibling-of-current= |
| f   | =my/zettel-find=              | =denote-sequence-find=                   |
| j   | =my/zettel-next-sibling=      | =denote-sequence-find-next-sibling=      |
| k   | =my/zettel-previous-sibling=  | =denote-sequence-find-previous-sibling=  |
| d   | =my/zettel-dired=             | =denote-sequence-dired=                  |
| p   | =my/zettel-dired-prefix=      | =denote-sequence-dired= with =C-u=       |
| P   | =my/zettel-dired-prefix-depth= | =denote-sequence-dired= with =C-u C-u=  |
| l   | =my/zettel-link=              | =denote-sequence-link=                   |
| r   | =my/zettel-reparent=          | =denote-sequence-reparent=               |
| a   | =my/zettel-adopt=             | =denote-sequence-rename-as-parent=       |

=j= and =k= are marked =:transient t= so the menu stays open while
paging through siblings.

**** Why =p= and =P= exist instead of =C-u=

=denote-sequence-dired= is documented to prompt for a sequence prefix
under =C-u= and for prefix plus depth under =C-u C-u=. That does not
work through a transient menu: a transient prefix consumes the
universal argument for its own purposes, so a =C-u= typed before
=C-c n z= never reaches the suffix command and the Dired buffer is
always built with prefix =ALL= and depth =ALL=.

The menu therefore binds three separate commands, each of which
let-binds =current-prefix-arg= around the =call-interactively= rather
than hoping the argument survives the menu. Called directly with
=M-x=, =my/zettel-dired= still honours =C-u= normally.

An empty string at the prefix prompt means no filtering, which is the
documented way to reach the depth prompt without narrowing by prefix.

**** Stale listings: previous invocations are killed first

Invoking =denote-sequence-dired= a second time with a different prefix
shows the *previous* invocation's file list. The buffer name updates
to the new filter while the contents do not, so the listing runs one
step behind and the correct one only appears after requesting the same
prefix twice.

The cause is upstream. =denote-sequence-dired= ends in

: (denote-sort-dired--prepare-buffer directory files-fn dired-name buffer-name)

where =directory= is identical for every invocation within one silo
and =files-fn= is a lambda that computes the listing. An existing
Dired buffer for that directory is reused and renamed rather than
rebuilt from =files-fn=.

=my/zettel-dired= therefore calls =my/zettel--kill-stale-dired-buffers=
before every invocation, which kills any Dired buffer whose name
matches =my/zettel--dired-buffer-regexp= (the literal =prefix ...;
depth= wording that =denote-sequence-dired= puts in its buffer names).
With nothing left to reuse, each invocation builds its list fresh.

To confirm the diagnosis independently, run =M-x denote-sequence-dired=
directly with =C-u=, twice with different prefixes: the bug reproduces
without any of this configuration's wrappers involved.

**** Dired shows file names, not titles

This is inherent to Dired, which lists a directory. Two things make
it readable, both enabled globally in =04-denote.el=:
=denote-dired-mode= font-locks the file name components (identifier,
signature, title, keywords) in distinct faces, and
=dired-hide-details-mode= strips the permission/owner/size/date
columns.

For an actual title-based view of a branch, use a =denote-sequence=
dynamic block in a structure note, which renders Org links showing
titles:

#+begin_example
,#+BEGIN: denote-sequence :sequence "1" :depth 2
,#+END:
#+end_example

=:sequence= is required, =:depth= optional; an empty string or =".+"=
collects every sequence. Refresh with =C-c C-x C-u=. Note the
tradeoff already recorded elsewhere: =:sequence= is a hard-coded
string, so a block pointing at a branch becomes wrong if that branch
is later reparented.

=l= differs from the ordinary =C-c d l= (=denote-link=) only in
narrowing minibuffer completion to files that *have* a signature.
Regular link commands still work on sequence notes.

=a= promotes an existing plain pks note into the Zettelkasten by
making it a new parent; follow with =r= to place it under an existing
thread.

** Workflow
:PROPERTIES:
:CUSTOM_ID: zettelkasten-workflow
:END:

1. *Start a thread* — =C-c n z n=, then the usual Denote title/keyword
   prompts. Write the note atomically and in your own words: one idea,
   self-contained enough to be read years later without context.
2. *Branch* — while inside that note, =C-c n z c= creates a child for a
   concept that deserves its own note. Link to it from the parent
   (=C-c n z l=).
3. *Continue* — =C-c n z s= from a note creates its sibling, extending
   the same line of thought rather than descending into it.
4. *Navigate* — =C-c n z f= visits a parent/sibling/child of the
   current note; =j=/=k= page between siblings; =C-c n z d= shows the
   whole tree in Folgezettel order (regular Dired sorts by =ls= flags
   instead, which scrambles sequences).
5. *Refactor* — =C-c n z r= moves a note (and its position in the
   hierarchy) under a different parent when its real home turns out to
   be elsewhere.

Structure notes (maps of content) remain the primary navigation aid;
sequences complement rather than replace them. A dynamic block scoped
to one branch gives a self-updating table of contents for that thread:

#+begin_example
,#+BEGIN: denote-links :regexp "==3.*_cognition" :sort-by-component signature
,#+END:
#+end_example

Refresh with =C-c C-x C-u= on the =#+BEGIN= line.

Functions: =my/zettel-*=, =my/zettelkasten-menu=, =my/zettel--in-pks=
| Module: =modules/22-zettelkasten.el= | Upstream manual:
https://protesilaos.com/emacs/denote-sequence

* Readwise (C-c n t r)
:PROPERTIES:
:CUSTOM_ID: menu-readwise
:END:

Imports Readwise highlights into one Org file per book, kept in
=my/readwise-directory= (=~/Downloads/readwise/=).

| Key       | Command                     | Effect                          |
|-----------+-----------------------------+---------------------------------|
| =C-c r s= | =my/readwise-sync=          | Incremental sync (=C-u= = full) |
| =C-c r r= | =my/readwise-review=         | Books with unprocessed quotes  |
| =C-c r o= | =my/readwise-open-directory= | Import folder in Dired         |
| =s= / =S= | in the menu                 | Incremental / full sync         |

** Why the files live outside the notes tree

=denote-directory= is the notes root, so anything beneath it is
visible to every Denote command, every dashboard and every search here.
Readwise material is raw input awaiting triage, not part of the
collection, so it sits outside — invisible by default rather than
visible-with-exclusions.

The files are therefore disposable: nothing links to them, and personal
commentary is stored by Readwise in the highlight's =note= field and
returns on every sync, so a book file can always be regenerated
exactly. Losing the directory costs only the time to re-sync.

** Why sync is two-phase

=updatedAfter= returns each book carrying only its *changed*
highlights. Writing that straight to disk would replace a complete file
with a partial one. So phase one asks which books changed, and phase
two re-fetches those books in full and rewrites their files. Every file
stays a complete picture of Readwise's current state.

The sync timestamp is recorded *before* fetching: a highlight changed
while the sync runs would otherwise fall between the fetch and the
timestamp and never be seen again.

File names derive from =user_book_id=, not import time, so re-import
overwrites rather than accumulating a copy per sync.

** File format

#+begin_example
,#+name: rw-710138927
,#+rw_location: s. 57
,#+rw_url: https://readwise.io/open/710138927
,#+begin_quote
The principle of GTD is to collect everything...
,#+end_quote

Personal commentary, when there is any.
#+end_example

The Readwise id sits on a =#+name:= affiliated keyword rather than in a
heading with a property drawer. These quotes have no natural title; one
would have to be invented at import and discarded at promotion.

Personal notes go *after* the quote block. A heading at column zero
inside =#+begin_quote= terminates the enclosing section, leaving the
block unclosed as far as =org-element= is concerned even though it
still renders acceptably — which is what an earlier hand-rolled import
of this material did.

** Review and promotion

=C-c r r= (or =r= in the menu) lists imported books that still have
unprocessed quotes. Selecting one lists that book's quotes.

*** Book list

A =tabulated-list-mode= buffer, which supplies column sorting and
alignment without hand-drawing either.

| Key       | Effect                                    |
|-----------+-------------------------------------------|
| =RET=     | Open the book's quotes                    |
| =mouse-1= | Same                                      |
| =S=       | Sort by the column at point               |
| =/=       | Filter by author or title (live)          |
| =C-/=     | Clear the filter                          |
| =g=       | Rebuild from disk and pks                 |
| =q=       | Bury                                      |

Columns: unprocessed count, total, author, import date, title.

Sort choice and filter both persist across entering a book and
returning, since both are held in globals.
=tabulated-list-sort-key= is buffer-local and cleared by
=define-derived-mode=, so a chosen sort would otherwise reset on every
return.

=/= narrows as you type. Author and title are matched as a single
string, so a search need not know which field a word lives in — the
same author recorded as "Emil Cioran" in one book and "Cioran Emil" in
another both match =cioran=. =C-g= restores the previous filter.

An active filter shows in the mode line as =[/text]= and is named in
the message after a rebuild: a persistent filter that were invisible
would make a partial list look complete.

*** Quote list

Four actions, available from the keyboard or from the per-quote
buttons beneath each quote:

| Key   | Effect                                                  |
|-------+---------------------------------------------------------|
| =RET= | Create a note, stay in the list                         |
| =o=   | Create a note and show it beside the list               |
| =z=   | Create a note, then run the Folgezettel commands on it  |
| =a=   | Add the quote to an *existing* note as cited evidence   |
| =g=   | Rebuild the list                                        |
| =q=   | Back to the book list                                   |

Each quote line shows its id, location and a clickable Readwise URL.

The action buttons carry a marker pointing at the quote button rather
than their own copy of the record, so the done state lives in one
place and marking it disarms all five entry points at once.

*** Consumed versus cited

A quote can reach =pks= two ways, and they mean different things:

| Written into the note | Meaning                    | On the list        |
|-----------------------+----------------------------+--------------------|
| =:RW_ID: rw-N=        | the note IS that quote     | disappears         |
| =#+name: rw-N=        | the note QUOTES it         | stays, =[CYTOWANY]= |

The second case is the book-note workflow: a note analysing a book
cites passages as evidence, and that must not use them up — each
remains available to become an atomic note of its own. Distinguishing
the two costs one extra regexp and avoids the failure mode where
citing a passage silently hides it from review.

*** What promotion writes

Prompts for a title, tags, and whether to carry over the personal
comment. The comment prompt defaults to yes, so a bare =RET= accepts
it; =y-or-n-p= has no notion of a default answer, so it reads a string
instead and treats anything starting with =n= as a refusal.

The note embeds the source rather than linking to it:

#+begin_example
,:PROPERTIES:
,:RW_ID: rw-710138927
,:RW_URL: https://readwise.io/open/710138927
,:SOURCE_AUTHOR: Sönke Ahrens
,:SOURCE_TITLE: How to Take Smart Notes
,:SOURCE_LOCATION: s. 20
,:END:
#+end_example

Embedding is required by the design: the imported file is disposable
and may be regenerated or deleted at any time, so a link would
eventually dangle.

*** How "already processed" is decided

=my/readwise--promoted-ids= scans =pks= once and classifies every
=rw-<id>= it finds. Since promotion writes the id into the note, pks
is the single source of truth — and it survives losing the whole
import directory, which an index kept beside those disposable files
would not.

One pass over pks rather than one grep per quote: a book can hold
hundreds of highlights, so per-quote scanning would be quadratic.

*** When the list is marked and when it is rebuilt

=RET= and =a= only mark the used quote (dimmed, labelled, disarmed),
because both are for working down the list and a rebuild would lose
the position.

=o= and =z= rebuild instead, so the consumed quote disappears rather
than lingering struck through — affordable precisely because attention
moves to the new note anyway.

*** Multiple authors

Readwise's =author= is free display text with no separator convention;
one book arrived as "Neil BrowneStuart M. Keeley". The importer passes
it through unchanged rather than splitting heuristically, which would
misfire on double-barrelled names, particles and initials. Correction
belongs in Readwise, where a re-sync then rewrites the file.

The structural answer, if wanted later, is to record a Citar key on
the promoted note and let Zotero be authoritative for bibliography
instead of duplicating an unreliable string.

** Configuration

| Variable                    | Purpose                                  |
|-----------------------------+------------------------------------------|
| =my/readwise-directory=     | Where book files are written             |
| =my/readwise-categories=    | Which Readwise categories to keep        |
| =my/readwise-file-keywords= | Denote keywords added to every file      |
| =my/readwise-state-file=    | Last-sync timestamp                      |

=my/readwise-categories= defaults to =("books")=; adding =articles= is
a one-item change and needs no code edit.

The state file lives *beside* the imported files rather than in
=.emacs.d=, deliberately: losing the directory should lose the state
too, so the next sync re-imports everything instead of skipping books
whose files are gone.

Token comes from =auth-source=:

: machine readwise.io login apikey password YOUR-TOKEN

Module: =modules/24-readwise.el=

* Inbox: migrated Obsidian notes (C-c n t i)
:PROPERTIES:
:CUSTOM_ID: menu-inbox
:END:

Review queue for the notes converted from the Obsidian vault. Journal
notes went straight into the =journal= silo; everything else landed in
=my/inbox-directory= (=~/notes/inbox/=) because each one needs a
decision — =pks=, =docu=, Zettelkasten, or nothing.

| Key       | Command                | Effect                                |
|-----------+------------------------+---------------------------------------|
| =C-c n t i r= | =my/inbox-review=  | Open the review list                  |
| =C-c X=, =C-c n t i x= | =my/inbox-extract= | Split a fragment into a *new* note |
| =C-c n t i o= | =my/inbox-open-directory= | Inbox folder in Dired         |

*Extraction is not promotion.* =x= takes a region, subtree or paragraph
out into a *new* note — new identifier, today's date, =:extracted_from:=
naming the source — and leaves a link where the fragment was. The
source note stays in the inbox, which is why it does not disappear.

Filing a note into a silo is =p= or =d= *inside the review list*. Those
act on the row at point, which is why they are not on the menu.

In the review list:

| Key   | Effect                                                       |
|-------+--------------------------------------------------------------|
| =RET= | Preview in a side window (=view-mode=, =q= closes)           |
| =e=   | Open for editing                                             |
| =p=   | Accept into =pks= (repairs journal links to it)              |
| =d=   | Accept into =docu= (same)                                    |
| =z=   | Move to =pks=, open, run =my/zettel-adopt=                   |
| =t=   | Edit tags (=denote-rename-file=)                             |
| =k=   | Reject → =~/notes/inbox-odrzucone/= (confirmed)              |
| =/=   | Filter by title, tags or source folder; =C-/= clears         |
| =g=   | Refresh; =S= or a header click sorts; =q= quits              |

** Extraction stops at the front matter

The paragraph fallback used to take it. =#+key:= lines are contiguous,
so with point anywhere among them the surrounding "paragraph" *is* the
front matter block — and since extracted text is replaced by a link,
extraction wrote the source's header into a new note and *removed it
from the source*.

=my/inbox--content-start= now marks where the body begins: past the
leading =#+key:= lines and any =:PROPERTIES:= drawer beneath them.

- point in the body, paragraph reaching too far up → the start is
  clamped;
- point in the front matter → refused, with a message;
- nothing but whitespace selected → refused.

If a note was extracted from while point sat in its header, check it:
the identifier survives in the file name, but =#+title:=,
=#+filetags:= and =#+identifier:= may need restoring.

** Why the inbox is not a silo

=denote-directory= is the notes root, so every subdirectory beneath it
is visible to every Denote command, dashboard and search. Half-reviewed
imports showing up there would defeat the point of staging, so the
module sets =denote-excluded-directories-regexp= to ="inbox"=. That
also matches =inbox-odrzucone/= as a substring, which is intended:
rejected notes should be less visible still.

Rejection *moves* rather than deletes. Several hundred decisions taken
over weeks will contain mistakes; the folder gets emptied once, at the
end.

** Columns that come from the migration

=Status= shows the Obsidian =status:= field (117 of the notes are
marked =draft=). =Folder= shows =:source_path:=, the note's original
vault location — which carries real classification information:
=00 Studia/Rok II/...= is =docu= material, =00 ZK/...= is not. Both are
read from the front matter without visiting the file.

** Accepting a note repairs journal links to it

The conversion could not resolve wikilinks pointing at non-journal
notes, because those notes did not exist in Org yet — and might end up
rejected. So the repair happens on accept: =p=, =d= and =z= scan the
journal silo for =[[Title]]= and =[[original-md-filename]]=, including
=#heading= and =|alias= variants, and rewrite them to
=[[denote:IDENTIFIER][...]]=. The number repaired is reported in the
echo area.

This runs without asking. At this volume a prompt per accept would be
noise, and the rewrite only ever touches links naming that exact note.

** Extraction (C-c X)

The counterpart to promoting a whole note: many fleeting notes hold one
paragraph worth keeping among three that are not.

=my/inbox-extract= takes the region; or the whole subtree when point is
on a heading; or the paragraph otherwise. It prompts for title (default:
first line of the selection), keywords and target silo, writes a new
Denote note recording =:extracted_from:=, and replaces the extracted
text with a =denote:= link — so the source keeps its context instead of
quietly losing a passage.

Bound to =C-c X=, not =C-c x=: the latter is =my/zotero-menu= globally,
and the inbox minor-mode map would shadow it in exactly the buffers
where a bibliography lookup is likely. The command also works in any
Org buffer via =M-x=.

** Configuration

| Variable                      | Meaning                          |
|-------------------------------+----------------------------------|
| =my/inbox-directory=          | Staging folder to review         |
| =my/inbox-reject-directory=   | Where =k= moves notes            |
| =my/inbox-silos=              | Silos =p= / =d= can accept into  |
| =my/inbox-journal-directory=  | Silo scanned for link repair     |

Module: =modules/25-inbox-review.el=

* Writing Projects (C-c n p)
:PROPERTIES:
:CUSTOM_ID: writing-projects
:END:

The management layer for a long-form text — tasks, deadline, time
spent, and which notes belong to it. Not a writing environment: it
holds no prose and does no exporting.

A project is a directory under =my/writing-projects-directory=
(=~/projects/=) containing a hub file named after it, plus =email/=.
The written text is *not* there: it stays in the Denote silos and
carries a =#+project:= line. Notes are knowledge that outlives the
project; the hub, its tasks and its mail are scaffolding that dies with
the deadline.

| Key | Command                              | Effect                       |
|-----+--------------------------------------+------------------------------|
| =n= | =my/writing-project-new=             | Create directory, hub, git   |
| =o= | =my/writing-project-open=            | Open a hub file              |
| =D= | =my/writing-project-dired=           | Project directory in Dired   |
| =f= | =my/writing-project-new-note=        | Create a note already joined |
| =a= | =my/writing-project-add-note=        | Add current note to project  |
| =m= | =my/writing-project-refresh-mentions=| Regenerate the mentions list |
| =t= | =my/writing-project-refresh-titles=  | Resync stale link captions   |
| =R= | =my/writing-project-rebuild-materials= | Rebuild membership by scan |
| =p= | =my/writing-project-progress=        | Chars / target / days left   |
| =i= =c= =z= =g= | =org-clock-*=            | In, out, cancel, goto        |
| =A= =U= | =org-agenda=, update files       | Agenda over hub files        |

** Creating versus adding

=f= asks for a project, a section and a title, then does everything
else: creates the note in =my/writing-project-note-directory=
(=pks/=, not the project directory), writes =#+project:=, adds the
Denote keyword, and links it from the hub. =a= does the same for a note
that already exists.

Project text lives in the silo like any other note. A chapter draft is
still knowledge: it is searched, linked and transcluded with everything
else, and it stays useful once the project is finished.

Membership is *additive*. A note already belonging to other projects
keeps them and gains the new one — the =#+project:= line takes several
slugs separated by spaces. Notes are routinely material for more than
one text, and silently dropping an older membership would be worse than
any duplication.

** Belonging versus mentioning

Two relations, two mechanisms, deliberately not merged:

| Relation  | Mechanism                              | Where it shows |
|-----------+----------------------------------------+----------------|
| belongs   | =#+project: slug= + link in the hub    | Materials      |
| mentions  | Denote keyword =slug= in the file name | Mentions       |

A journal entry keyworded =licencjat= records that something about the
thesis happened that day. It is not a chapter. Listing it as project
material would make the material list useless, so mentions are a
separate section and =a= is what promotes one into Materials.

Because =f= gives new notes the project keyword too, a member would
otherwise appear in both lists. =m= subtracts current members, so what
remains under Mentions is exactly the set of candidates for promotion —
which is the only reason to read that section.

=a= writes *both* halves: the keyword into the note, the link into the
hub. The hub is the fast path — one file, read instantly. The keyword
is repair data: if the hub is lost, =R= reconstructs it by scanning the
silos. Scanning is never on the path of opening a project.

** Links survive renaming

Hub entries are =[[denote:IDENTIFIER][Title]]=. A Denote identifier is
assigned at creation and never changes, so adding a tag to a chapter —
which rewrites its file name — breaks nothing. Only the caption goes
stale, since it is a copy of the title made when the link was written.
=t= resyncs captions from the current titles; nothing depends on them.

*Warning:* this does *not* extend to =#+INCLUDE:=, which takes a path
and does break on rename. Assembling a manuscript from several chapter
files needs identifiers resolved to paths at export time. That belongs
to the export layer, not here.

** Why a keyword and not an Org property

Org properties live in a drawer under a heading, and Denote notes start
with =#+title:= and usually have no top-level heading — a file-scope
property would need =#+PROPERTY: PROJECT foo=. A =#+project:= keyword
matches what Denote already writes, takes several values separated by
spaces, and is greppable:

: rg '^#\+project:.*licencjat' ~/notes

** What counts towards the target

Only files linked under the =Text= heading. Sources are excluded
because they are not the deliverable.

Counting runs =pandoc -f org -t plain= and measures the result, not the
Org source: front matter, drawers, citation syntax and =:noexport:=
subtrees can be a fifth of a file, and a publisher counts what the
reader receives. Results are cached on each file's modification time,
so pandoc runs once per edit rather than once per look. Set
=my/writing-count-with-pandoc= to nil to fall back to a cruder in-Emacs
count, then run =my/writing-clear-char-cache=.

A link that resolves to no file is reported as =unresolved= rather than
silently dropped — that means a note was deleted or its identifier
changed.

** No drafting/editing phase flag

The progress line changes meaning once the target is passed:

: licencjat: 7350 / 20000 chars (37%, 4 files) · 23 days · 550 chars/day
: licencjat: 21200 / 20000 chars (106%, 9 files) · 23 days · 1200 chars over target

An explicit phase flag was considered and rejected: drafting and
editing alternate from one session to the next, so the flag would spend
most of its life set wrongly. One comparison covers both.

** Agenda is project hubs only

=org-agenda-files= is the hub file of every project, rebuilt by =U= and
on project creation. The previous setting in =05-notes.el= pointed at
the three silos plus the capture file, so every agenda build read some
3,700 notes to find no tasks at all — or worse, to find the =TODO=
examples in =docu/= and present them as work.

Personal tasks are not carved out as an exception. A project named
"Life" is a project like any other.

** Clocking, and what Wayland breaks

Clocking is opt-in per heading: nothing is measured until =C-c C-x C-i=
on a task, so this cannot leak into unrelated Emacs use.

| Setting                          | Effect                              |
|----------------------------------+-------------------------------------|
| =org-clock-persist= = ='history= | A session outlives Emacs            |
| =org-clock-idle-time=            | Prompt on return after N minutes    |
| =org-clock-auto-clockout-timer=  | Stop by itself after N seconds      |
| =org-time-stamp-rounding-minutes= | =S-<up>= on a CLOCK line moves 5 min |

*Warning:* under Wayland none of the idle handling is fully reliable.
Org can ask macOS and X11 how long the *user* has been idle (via
=xprintidle=); under Wayland it can only ask Emacs, so reading in a
browser with an Emacs frame focused is counted as work.

Two fallbacks. Manual correction is the reliable one: put point on a
timestamp inside a =CLOCK:= line and =S-<up>= / =S-<down>= moves it in
five-minute steps, with Org recomputing the =\=> H:MM= total. =C-c C-x
C-q= cancels a clock outright.

The other is =my/writing-use-mutter-idle=, which takes idle time from
GNOME Mutter over D-Bus. *Disabled by default and unverified.* Check it
works before enabling:

: dbus-send --print-reply --dest=org.gnome.Mutter.IdleMonitor \
:   /org/gnome/Mutter/IdleMonitor/Core \
:   org.gnome.Mutter.IdleMonitor.GetIdletime

=04-denote.el= used to set =org-clock-persist-file= to nil. That block
is now inert: a nil file name makes =org-clock-persistence-insinuate=
fail on the first clock change.

** Configuration

| Variable                              | Meaning                        |
|---------------------------------------+--------------------------------|
| =my/writing-projects-directory=       | Root of project directories    |
| =my/writing-project-subdirectories=   | Created inside a new project   |
| =my/writing-project-silos=            | Scanned by =R= and =m=         |
| =my/writing-project-note-directory=   | Where =f= creates notes        |
| =my/writing-keyword-new-notes=        | Also tag new notes with slug   |
| =my/writing-count-with-pandoc=        | Exported text vs crude count   |
| =my/writing-clock-idle-minutes=       | Ask after this much idleness   |
| =my/writing-clock-auto-clockout-seconds= | Stop by itself; nil disables |
| =my/writing-use-mutter-idle=          | D-Bus idle query (unverified)  |
| =my/writing-heading-*=                | Hub heading names              |

*Warning:* the =my/writing-heading-*= variables name the headings the
commands look for. Changing one after projects exist orphans that
section in every hub already created. Set them before the first
project, or rename the headings in every hub at the same time.

** Why projects live outside the notes tree

=~/notes= is synchronised by Syncthing and =~/projects/= is not.
Project directories are meant to be independent git repositories, and
Syncthing replicating =.git/= across devices can corrupt it — index,
locks and packfiles are written concurrently with no atomicity
guarantees. =~/notes/.stignore= excludes =.git= for the same reason.

Module: =modules/28-writing-projects.el=

* Office Export — ODT and DOCX (C-c n x)
:PROPERTIES:
:CUSTOM_ID: office-export
:END:

For text that leaves Emacs: a chapter for a supervisor, an article for
an editor.

| Key | Command                     | Effect                        |
|-----+-----------------------------+-------------------------------|
| =O= | =my/org-export-to-odt=      | ODT via Org's own exporter    |
| =W= | =my/org-export-to-docx=     | ODT, then LibreOffice → DOCX  |
| =?= | =my/office-check-setup=     | Report what is missing        |

Output goes to =~/projects/<slug>/export/= when the file declares
=#+project:=, otherwise to =~/notes/export/<silo>/=. A document is a
deliverable of its project, not of the notes tree.

** Why not pandoc

Pandoc reads Org and writes DOCX in one step, and does produce real
Word footnotes from org-cite citations. It is still the wrong tool
here, for two reasons found by testing rather than assumed:

- Its Org reader is a reimplementation. =#+INCLUDE: "f.org::*Heading"=
  is unsupported — pandoc warns and *omits the content*. =:only-contents
  t= is ignored, so the heading comes through anyway. Both are how
  =20-transclusion.el= writes INCLUDE pairs, so an assembled manuscript
  would export short without failing.
- It renders citations with its own citeproc. PDF goes through Org and
  citeproc-el; routing DOCX through pandoc lets one source produce two
  different citation apparatus, and the difference would surface only
  in the copy already sent.

Org exports to ODT, LibreOffice converts. Same exporter as PDF, same
INCLUDE handling, same citation renderer.

** ODT or DOCX

Word has opened ODT since 2007 and OnlyOffice reads it natively, so ODT
is usually enough and skips the conversion. Reach for DOCX when the
document is coming *back*: tracked changes and comments made in Word
survive a DOCX round-trip more reliably than an ODT one.

Note that neither direction merges. A supervisor's tracked changes
cannot be applied to the Org source automatically — they are read and
retyped. The exported file is a copy, never a second master.

** Citations

Handled by org-cite and CSL, the same as PDF. With a note style such as
=chicago-notes-bibliography.csl= the citations become real footnotes and
"ibid." is resolved from actual adjacency in the exported text, so
reordering paragraphs fixes the back-references instead of breaking
them.

*Warning:* a discursive footnote sitting between two citations can
confuse CSL position tracking, producing "ibid." where the short form
belongs. Proofread citation runs in passages with dense commentary
footnotes.

** Configuration

| Variable                        | Meaning                        |
|---------------------------------+--------------------------------|
| =my/office-output-dir=          | Root for non-project exports   |
| =my/office-project-subdirectory=| Subdirectory inside a project  |
| =my/office-convert-timeout=     | Seconds to wait for LibreOffice |

Module: =modules/29-writing-export.el=

* Citation Key Checker (C-c b c)
:PROPERTIES:
:CUSTOM_ID: cite-check
:END:

=my/cite-check-keys= scans the notes tree for citation keys that no
longer exist in the bibliography, and lists each one with clickable
locations.

** Why it is needed

Better BibTeX generates keys from metadata, so correcting a year or an
author can rewrite the key of an item already cited in finished text.
Unchecking "Regenerate citation key when item changes" in the Better
BibTeX preferences prevents that going forward, but says nothing about
keys that already drifted, or about items added before the setting was
changed. Pinning a key (a =Citation Key:= line in an item's =Extra=
field) is stronger still, since it survives a rebuild of the BBT
database.

The failure itself is loud, not silent: a stale key exports as
=NO_ITEM_DATA=. But it only becomes visible once the document has been
produced, which may be long after the writing. This checks on demand
instead.

** Commands

| Command                | Effect                                        |
|------------------------+-----------------------------------------------|
| =my/cite-check-keys=   | report unresolved keys (=C-c b c=)            |
| =my/cite-rename-key=   | replace a key across all scanned files        |

=my/cite-rename-key= exists because the usual cause is a rename in
Zotero rather than a typo, so the same stale key tends to appear in
several places. It rewrites keys only *inside* citations — the same
string in ordinary prose is untouched, which matters because keys
often look like ordinary words.

=my/cite-check-directories= narrows the scan; nil means the whole
notes tree.

* Zotero / Bibliography (C-c x)
:PROPERTIES:
:CUSTOM_ID: menu-zotero-detail
:END:

Also accessible from =C-c n t z=. The =C-c x= binding remains active
for direct access without going through the notes menu.
Module: =modules/18-zotero-transient.el=

| Key | Function                    | Description                              |
|-----+------------------------------+-------------------------------------------|
| n   | =citar-create-note=         | New note from a Zotero reference         |
| o   | =citar-denote-open-note=    | Open existing bibliographic note         |
| f   | =my/open-bib-pdf-right=     | Open PDF for current note (right split)  |
| e   | =citar-open-entry=          | Open BibTeX entry in refs.bib            |
| u   | =citar-open-links=          | Open URL or DOI in browser               |
| i   | =citar-insert-citation=     | Insert in-text citation [cite:@key]      |
| R   | =my/insert-full-reference=  | Insert full bibliography at point        |
| S   | =my/insert-short-reference= | Insert short reference (Author, Title)   |

* Capture System
:PROPERTIES:
:CUSTOM_ID: capture-system
:END:

Captures land in the file defined by =my-journal-captures= (variable
in =00-core.el=). Template =j= creates an entry under =* Ideas= with a
=:SOURCE:= property linking back to the originating note.

The right-side window behaviour is implemented via
=org-capture-mode-hook= calling =my/capture--show-right=, which splits
the origin window and places the capture buffer on the right. This
only fires when capture is started via =my/capture-idea=; generic
=org-capture= is unaffected.

Module: =modules/06-capture.el=

* Session Management (desktop)
:PROPERTIES:
:CUSTOM_ID: session-management
:END:

Session persistence via built-in =desktop-save-mode=. Open files and
cursor positions survive restarts. Desktop file lives in
=~/.emacs.d/desktop/=.

** =my/desktop-after-startup-hook=

An ordinary hook, run *once*, shortly after Emacs has finished starting
up — whether or not a session was restored.

Two Emacs hooks answer half the question each: =desktop-after-read-hook=
fires only when there was a desktop file to read, =emacs-startup-hook=
fires either way but too early relative to restoration. Both feed this
hook, and a flag makes sure only the first one counts. The delay before
firing is =my/desktop-startup-delay= (0.5 s), which lets the rest of
startup settle before anything reshapes windows.

Registered on it:

| Module | What it does |
|--------+--------------|
| =03-spelling.el= | Clears the Hunspell block and activates flyspell in every restored buffer |
| =15-workspace.el= | Opens the notes dashboard in its tab |

Both registrations are guarded: without =01-ui.el= they degrade to a
message or to =emacs-startup-hook=, rather than to silence.

This exists because the two consumers used to solve the problem
separately, inside =15-workspace.el= — two hooks, two timers, and a
"does the Dashboard tab exist already?" test whose only purpose was to
stop them colliding. Spell checking also passed through that file, so
removing the dashboard module disabled it. A module that owns the
session can say when the session is ready; it should not have to know
what anyone does with that fact.

Functions: =my/desktop--run-startup-hook= | Variables:
=my/desktop-after-startup-hook=, =my/desktop-startup-delay= |
Module: =modules/01-ui.el=

Three-layer strategy:

| Layer    | When       | What happens                                       |
|----------+------------+----------------------------------------------------|
| 1. Trim  | on exit    | keep only the 10 most-recently-used file buffers   |
| 2. Eager | on startup | restore up to 10 buffers synchronously             |
| 3. Lazy  | idle time  | restore the rest in the background                 |

The trim limit is =my/desktop-max-buffers= (currently *10*); the eager
limit is =desktop-restore-eager= (currently *10*). To keep more
buffers between sessions, change only =my/desktop-max-buffers= — with
the trim limit equal to the eager limit, the lazy layer is dormant,
and raising the trim limit re-activates it automatically.

** Trim: what counts as "old"
:PROPERTIES:
:CUSTOM_ID: fn-my-desktop-trim-buffers
:END:

=my/desktop-trim-buffers= runs from =desktop-save-hook= before every
save and kills file buffers beyond the limit. Recency is *most
recently used* (the order of =buffer-list=, where selecting a buffer
moves it to the front) — not file modification time on disk. This was
changed deliberately: mtime ordering killed buffers that were being
read but not edited, while keeping files an external process happened
to touch.

Buffers whose major mode is in =desktop-modes-not-to-save=
(=pdf-view-mode=, =nov-mode=, =fundamental-mode=) or whose file
matches =desktop-files-not-to-save= (=.aux=, =.log=, =.out=, =.toc=,
=.tex=, =.epub=) are skipped entirely: they are never persisted, never
counted toward the limit, and never killed by the trim.

*Protected buffers* (added this session) are excluded from
consideration entirely — never killed, never counted toward the
10-buffer limit:

- Buffers open in a tab-bar tab. For the current tab this means
  whatever's actually shown in a window right now. For every *other*
  tab, only the =my/desktop-tab-protect-depth= (default *3*)
  most-recently-used buffers in that tab count — not that tab's full
  history. A tab's own buffer-list (=wc-bl=, what =tab-bar= restores
  when you switch to it) only ever grows for as long as the tab
  lives, so protecting all of it would eventually shield hundreds of
  stale buffers from a month-old tab. Taking just the front of that
  MRU list keeps the protected set to roughly
  =(number of tabs) × my/desktop-tab-protect-depth=, regardless of
  tab age. Closing a tab does not itself kill any buffers — it only
  removes that tab's protection, so its buffers become ordinary trim
  candidates again on the next save.
- Buffers manually pinned via =my/desktop-keep-buffer= (see below).
- Buffers whose file matches =my/desktop-always-keep-regexps=. This
  layer exists because a pure MRU policy has a systematic blind spot:
  a reference document consulted daily but rarely *selected* keeps
  falling out of the recency window and being killed, while a note
  opened once to check something survives because it was touched
  most recently. Pinning fixes that per session and has to be
  remembered; a regexp fixes it permanently. Matched against the full
  file name, so a whole silo can be covered with
  =(regexp-quote my-notes-journal)=.

** C-c d p — What survives the next save
:PROPERTIES:
:CUSTOM_ID: fn-my-desktop-show-protected
:END:

Opens =*Desktop Survival*=, listing every open note buffer grouped by
why it does or does not survive: pinned, open in a tab, always-kept by
regexp, recent enough to fall within the trim limit, and — last — the
ones that will be killed at the next save.

Both this report and =my/desktop-trim-buffers= read the same
=my/desktop--classify-buffers=, so the two cannot disagree. A report
that contradicted the actual behaviour would be worse than no report.

Function: =my/desktop-show-protected= | Module: =modules/01-ui.el=

** Dashboard restoration at startup
:PROPERTIES:
:CUSTOM_ID: fn-desktop-dashboard-restore
:END:

=desktop-save= only persists file-visiting buffers. The Notes
Dashboard is generated text with no file behind it, so it is never
saved: after a restart the Dashboard tab returns from the restored
frame configuration, but its window points at a buffer that no longer
exists.

=my/desktop--open-dashboard-at-startup= rebuilds it from
=emacs-startup-hook= (depth 90, so it runs after the rest of startup).
Regenerating is exact rather than approximate, since the dashboard's
content is derived from the notes on disk anyway. Set
=my/desktop-open-dashboard-at-startup= to nil to disable.

Functions: =my/desktop-trim-buffers=, =my/desktop--protected-buffers=,
=my/desktop--tab-buffers= | Variable: =my/desktop-tab-protect-depth=
(customize group =my/desktop=) | Module: =modules/01-ui.el=

** C-c d k — Pin/unpin buffer against trim
:PROPERTIES:
:CUSTOM_ID: fn-my-desktop-toggle-keep-buffer
:END:

Toggles the buffer-local variable =my/desktop-keep-buffer= for the
current buffer. A pinned buffer is *never* killed by
=my/desktop-trim-buffers=, no matter how long ago it was last used —
independent of whether it's in a tab at all. A 📌 appears in the mode
line (=mode-line-misc-info=) for any pinned buffer, with a tooltip on
hover.

Can also be set permanently per-file via a file-local variable:
#+begin_example
;; Local Variables:
;; my/desktop-keep-buffer: t
;; End:
#+end_example

Function: =my/desktop-toggle-keep-buffer= | Module: =modules/01-ui.el=

** C-c d s — Save session now
:PROPERTIES:
:CUSTOM_ID: fn-my-desktop-save-now
:END:

Saves the desktop immediately without waiting for exit. Note: the
trim (and its buffer killing) also runs here, because it hangs on
=desktop-save-hook=.

Function: =my/desktop-save-now= | Module: =modules/01-ui.el=

* Undo and Backup History
:PROPERTIES:
:CUSTOM_ID: undo-history
:END:

Four independent layers, each answering a different question. Full
walkthrough with exercises:
=~/notes/docu/…historia-zmian-undo-i-kopie-zapasowe…org=

| Layer             | Question it answers                     | Horizon        |
|-------------------+-----------------------------------------+----------------|
| undo              | undo what I just did                    | seconds–hours  |
| persistent undo   | …and after restarting Emacs             | days           |
| numbered backups  | how the file looked before this session | last ~10 sessions |
| git               | how this note looked in March           | years          |

** vundo (C-x u)
:PROPERTIES:
:CUSTOM_ID: fn-vundo
:END:

Emacs's built-in undo is already a tree — undoing an undo is recorded
as a change rather than discarding the branch, so every previous buffer
state stays reachable. What was missing was a way to *see* it.

=vundo= draws that existing tree and navigates it; it does not replace
the undo system. Keymap, verified against =vundo-mode-map=:

| Key           | Effect                                            |
|---------------+---------------------------------------------------|
| =f= / →       | forward (newer state)                             |
| =b= / ←       | backward (older state)                            |
| =n= / ↓       | *at a fork only*: sibling branch below            |
| =p= / ↑       | *at a fork only*: sibling branch above            |
| =a=           | back to the nearest fork                          |
| =w=           | forward to the next fork                          |
| =e=           | to the tip of the current branch                  |
| =l= / =r=     | previous / next *saved* state                     |
| =m= / =u=     | mark / unmark a node for diff                     |
| =d=           | diff current node against marked (or its parent)  |
| =RET=         | *confirm* — keep the buffer in the chosen state   |
| =q= or ~C-g~  | quit and *roll back* what vundo did               |

*=n= and =p= only move at a branching point.* They switch between
branches sharing a parent, not up and down a list. On a linear stretch
they have nowhere to go and appear to do nothing; the arrow keys are
bound to the same commands, so they behave identically.

*The connecting lines are connectors, not paths.* This is what makes
the behaviour look broken: the drawn lines suggest a route you could
walk, but =n=/=p= *jump* vertically between nodes in the **same
column** — which is what siblings are, children of one fork drawn one
row apart at equal distance from it. To drop to the node below, you
must be standing directly above it. One step too far right and the
node below is no longer your sibling, so nothing happens.

: ──○──○──●──○──○      n works here (● is directly above)
:      │
:      └──○──○──○

: ──○──○──○──●──○      n does nothing here
:      │
:      └──○──○──○      (nothing in this column below)

The sequence that always works: =a= to reach the fork, =f= one step
in, then =n= or =p=. =a= also reveals where the fork actually is,
which is not always where the drawing suggests.

*=RET= commits, =q= reverts.* Moving through the tree changes the
buffer provisionally. =q= and ~C-g~ are bound to the *same* command
(=vundo-quit=) — there is no difference between them, and by default
both roll back. Set =vundo-roll-back-on-quit= to nil if you would
rather have =q= keep the current state and stop needing =RET=.

*=d= needs something to compare against.* The diff runs between the
current node and a marked node, or its parent when nothing is marked —
so at the root of the tree, with no mark set, there is nothing to
diff. Mark with =m= first, then move and press =d=.

This pairing was chosen over =undo-tree= specifically because
=undo-tree= substitutes its own undo implementation. It is also
incompatible with =undo-fu-session= — the two cannot be combined, since
undo-tree defines its own data structures.

** undo-fu-session
:PROPERTIES:
:CUSTOM_ID: fn-undo-fu-session
:END:

Writes undo history on save and restores it when the file is opened
again, so the tree survives restarting Emacs. Standalone: despite the
name it has no dependency on =undo-fu= and stores Emacs's built-in undo
data unchanged.

Data goes to =~/.emacs.d/undo-fu-session/=, capped at
=undo-fu-session-file-limit= (2000) files, oldest removed first. Kept
inside =.emacs.d= rather than beside the notes on purpose: it is
machine-local state, and syncing it would create conflicts over files
meaningless on the other device.

*Restoration is conditional.* Buffer length and checksum are verified
first; if the file changed outside Emacs — a Syncthing update from
another device, for instance — the history is not restored, because it
would describe different text than what is on disk.

** Numbered backups
:PROPERTIES:
:CUSTOM_ID: numbered-backups
:END:

*A backup is written on the first save of a buffer after visiting it,
not on every save.* This is the part that is easy to misread: with
=kept-new-versions= at 10, the ten kept files are roughly the last ten
*editing sessions*, not the last ten saves — usually far more history
than the number suggests, not less.

Notes go to =~/notes/.backups/=, everything else to
=~/.emacs.d/backups/=. File names are the original's full path with
=/= replaced by =!=, suffixed =.~N~=.

=M-x diff-backup= diffs the current file against its most recent
backup. Older versions open with =C-x C-f= like any text file.

In =bash= those names trigger history expansion. Quote the *whole*
name in single quotes (=cat '!home!…!bookmarks.~18~'=), not part of it,
or run =set +H=. =zsh= does not have this problem.

* Link Tooltips
:PROPERTIES:
:CUSTOM_ID: link-tooltips
:END:

Hovering the mouse over a =denote:= link used to stall Emacs for as
long as it took to list the whole =denote-directory=. This section
explains the cause and what was traded away to remove it.

Module: =modules/30-link-tooltips.el=

** The cause

Denote registers a =:help-echo= *function* for its Org link type:

#+begin_src emacs-lisp
(org-link-set-parameters "denote"
  :follow #'denote-link-ol-follow
  :help-echo #'denote-link-ol-help-echo
  ...)
#+end_src

Org copies whatever =:help-echo= holds into the =help-echo= text
property. The distinction that matters:

- a *string* is produced once, while the buffer is fontified;
- a *function* is called by redisplay every time the pointer lands on
  the link, and again as it moves across it.

=denote-link-ol-help-echo= resolves the identifier through
=denote-get-path-by-id=, which calls =denote-directory-files=, which
walks the tree with =directory-files-recursively=. Denote caches none
of it. The cost is one full recursive scan of =~/notes/= per pointer
movement — invisible at a few hundred notes, unusable at several
thousand.

** What is traded

The tooltip is informational: it names the file behind a link whose
title is already printed on screen. Paying the most expensive operation
in the note collection for that is a bad trade, so the parameter is
replaced.

Only the tooltip changes. =:follow= is untouched, so opening a link
still resolves the identifier against the current state of the tree,
and a note moved between devices by Syncthing is always found. A stale
tooltip can at worst name a path that has since changed, and nothing
acts on that text.

** Styles

=my/link-tooltip-style=:

| Value        | Shows                     | Disk access                          |
|--------------+---------------------------+--------------------------------------|
| =identifier= | =denote:20260802T101500=  | none (default)                       |
| =path=       | the resolved file name    | one scan per =my/link-tooltip-cache-seconds= |
| =default=    | Org's own =LINK: ...=     | none                                 |

=identifier= is the default because the path answers a question that is
rarely being asked.

Switching to or from =default= changes a decision Org makes while
fontifying, so =my/link-tooltip-set-style= runs =font-lock-flush= in the
current buffer. Other open Org buffers pick the change up when they are
next refontified.

** Why this cache is allowed

=04-denote.el= states that nothing caches =denote-directory-files=,
because a stale cache in a tree Syncthing writes into would advertise
notes that no longer exist. That still holds. The cache in this module
is keyed by identifier, is read only by the tooltip, is dropped whole
after =my/link-tooltip-cache-seconds= (30), and is unreachable from any
prompt, backlink buffer or search.

Failed lookups are cached alongside successful ones. An identifier with
no file behind it is a broken link, and retrying the scan on every
pointer movement is exactly the cost being removed.

** Reading the link under the pointer

The identifier comes from the =htmlize-link= text property, written by
=org-activate-links= and also the source Denote's own tooltip function
reads. A line-bounded regexp scan is the fallback for buffers fontified
by something else. Neither path touches the file system; neither cost
grows with buffer size.

** Configuration

#+begin_src emacs-lisp
(setq my/link-tooltip-style 'identifier)   ; identifier | path | default
(setq my/link-tooltip-cache-seconds 30)    ; only used by `path'
#+end_src

=M-x my/link-tooltip-clear-cache= empties the cache early; it is needed
only when a tooltip is expected to reflect a move made seconds ago.

** Related measurement

The same directory scan is behind every Denote prompt and backlink
buffer. To see what it currently costs:

#+begin_src emacs-lisp
(benchmark-run 3 (denote-directory-files))
(length (denote-directory-files))
#+end_src

If the count is far above the number of real notes, the tree contains
directories that should not be scanned at all — =.git=, Syncthing's
=.stversions=, attachment folders. =denote-excluded-directories-regexp=
is set to ="inbox"= in =25-inbox-review.el=; widening it there helps
every Denote operation, not only tooltips.

* Keyword Prompts
:PROPERTIES:
:CUSTOM_ID: keyword-prompts
:END:

Every prompt for tags in this configuration goes through one function,
=my/notes-read-keywords=, so that they all complete against the same
vocabulary and all behave the same way.

Function: =my/notes-read-keywords= | Module: =modules/05-notes.el=

Callers: =my/denote-base=, =my/denote-essay=, =my/denote-linked-note=
(05-notes.el), =my/capture-promote-to-note= (06-capture.el), Readwise
quote promotion (24-readwise.el). =my/denote-keywords-edit= applies the
same prompt behaviour to Denote's own =denote-rename-file-keywords=,
which is what =C-c n d k= runs.

=my/denote-journal= and =my/denote-journal-date= do not appear in that
list: they write =:journal:= and prompt for nothing.

** Completion instead of typing blind

Tags used to be read with =read-string= and split on spaces, with no
completion at all. With a few dozen keywords that was inconvenient;
after the Obsidian migration it is a source of near-duplicates —
=filozofia= beside =flozofia=, =kant= beside =kanta= — that nothing can
tell apart afterwards, because Denote treats every distinct string as
its own keyword.

=denote-keywords-prompt= is Denote's own prompt. It completes against
the vocabulary Denote infers from existing file names
(=denote-infer-keywords= is =t=, set in 04-denote.el), removes
duplicates and returns a list. Calling it rather than writing a private
prompt is what keeps these commands and =denote-rename-file-keywords=
identical to each other, and identical as Denote changes.

*Separator changed.* Keywords are now typed *comma-separated*, as
everywhere else in Denote, not space-separated. A keyword may therefore
contain a space, which the old prompt could not express.

** Creating a keyword that is a prefix of an existing one

Vertico preselects the first matching candidate, and =RET=
(=vertico-exit=) submits the *selected candidate*, not what was typed.
Typing =zuzi= where =zuzia= exists therefore filed the note under
=zuzia=. Only the element at point is completed, which is why placing
the new keyword anywhere but last in the list used to be the way
through.

Vertico's documented answer is =M-RET= (=vertico-exit-input=), which
submits the input as typed. It works in every prompt and keeps working
whatever is configured here.

=my/notes-keyword-preselect= makes that the behaviour of plain =RET=
inside these prompts by binding =vertico-preselect= around the call.
The binding is dynamic, so it covers the recursive minibuffer edit and
nothing else — no hook, no advice, no global state, and no separate
module loading later to correct a command that already ran.

Keys inside a keyword prompt:

| Key       | Effect                                                  |
|-----------+---------------------------------------------------------|
| =TAB=     | next candidate — the first press leaves the input line   |
| =S-TAB=   | previous candidate                                       |
| =,=       | insert the highlighted candidate and start the next keyword — the fast way to take several from the list |
| =C-TAB=   | insert the highlighted candidate *without* exiting, when the separator is not wanted yet |
| =RET=     | submit: the input when on the input line, the candidate when one is selected |
| =M-RET=   | submit the input regardless of selection (Vertico's own) |

Choosing several existing keywords is therefore *filter, TAB, comma,
repeat*. On the input line, with nothing highlighted, =,= stays an
ordinary comma, so typing a new keyword by hand is unaffected.

=TAB= is Vertico's =vertico-insert= by default. It is moved here
because a list appearing under a half-typed word reads as something to
step through, and =TAB= is the key hands reach for to do that. The
rebinding is confined to these prompts: file-name completion elsewhere
keeps =TAB= as insert, where it matters most.

*Not M-TAB.* Insertion was bound there first and could never fire:
GNOME owns =Alt-Tab= as the window switcher, so the compositor consumes
the event and Emacs never sees it. Worth remembering before reaching for
=Alt= in any binding on this desktop — =C-h k= followed by the key
reports nothing at all when the compositor has taken it.

The capture destination prompt ([[#capture-filing][Capture Filing]]) shares =TAB=, =S-TAB=
and =C-TAB= through the same function but gets *no* =,= binding: it reads
a single heading, and a heading may contain a comma.

The rebinding is applied with =minibuffer-with-setup-hook=, the built-in
way to configure a single minibuffer session. The hook exists only for
the duration of the call, exactly like the =vertico-preselect= binding —
nothing is added to =minibuffer-setup-hook= globally. =:append= places
it after Vertico's own setup, so the local map it extends is Vertico's.

The cost is one keystroke when reusing an existing keyword. Whether
that is a good trade depends on how often new keywords are coined
against how often old ones are reused.

** Configuration

#+begin_src emacs-lisp
;; prompt = RET is literal (default); first = Vertico's own behaviour
(setq my/notes-keyword-preselect 'prompt)

;; nil = leave Vertico's own TAB/arrow bindings alone in these prompts
(setq my/notes-keyword-tab-navigates t)
#+end_src

Without Vertico the setting has no reader and the prompt is Denote's
unmodified. Without Denote, =my/notes-read-keywords= falls back to the
old space-separated =read-string=, so note creation still works.

** Why the vocabulary prompt is not instant

=denote-keywords-prompt= builds its candidate list from
=denote-directory-files=, which walks the whole notes tree. That is the
same cost described in [[#link-tooltips][Link Tooltips]] and the same measurement applies:

#+begin_src emacs-lisp
(benchmark-run 3 (denote-directory-files))
#+end_src

* Org Faces and Fonts
:PROPERTIES:
:CUSTOM_ID: org-faces
:END:

Where the appearance of Org text is decided, and why it is decided in
exactly one place per kind of decision.

** The rule: three layers, no overlap

There is no single owner of "Org appearance". There are three, split by
*what kind of attribute* is being set, because the three behave
differently when the theme changes.

| Layer | Owns | Where |
|-------+------+-------|
| Colour | every foreground and background | =modus-themes-common-palette-overrides= in =09-theme.el= |
| Size, weight, typeface of headings | =org-level-0..8= and the agenda headings | =modus-themes-headings= in =09-theme.el= |
| Font families | =default=, =fixed-pitch=, =variable-pitch= | =03b-fonts.el= |
| The residue | a handful of faces no override reaches | =my/theme--custom-faces= in =09-theme.el= |

The split is not arbitrary. A *height* means the same thing under a
light and a dark theme; a *colour* does not. A face spec that mixes them
— which is what =custom.el= used to hold — is portable in one half and
broken in the other.

*Do not put Org faces in custom.el.* =M-x customize-face= writes there
and its result will beat =09-theme.el= for that face, which is fine for
trying something out and wrong as a resting place. Move anything worth
keeping into =09-theme.el= and delete it from =custom.el=.

** Why colour is a palette override and not a hex value

A palette override names an entry, not a value:

#+begin_src elisp
(fg-heading-1 blue-warmer)
(bg-prose-block-contents bg-yellow-nuanced)
#+end_src

Each theme decides what =blue-warmer= is for itself, so one line is
correct in =modus-operandi-tinted= and in =modus-vivendi-tinted= alike.
Writing ="#354fcf"= there would pin the light value into both.

The base of the override list is =modus-themes-preset-overrides-faint=,
a preset the package ships: toned-down greys, desaturated accents,
several grey backgrounds removed. That is where the pastel register
comes from. The explicit entries above the =,@= splice refine it.

To audition a different register, swap the preset for
=modus-themes-preset-overrides-cooler= or =...-warmer=, or drop it and
keep only the explicit entries. *Reload the theme afterwards* — press
=<f5>= twice. Overrides take effect when a theme loads, not when they
are assigned.

=M-x modus-themes-list-colors-current= shows every palette entry with a
preview, and =C-u= on it shows the semantic mappings, which is what an
override targets.

** Fonts: three base faces, one per-silo override

=03b-fonts.el= sets three faces for the whole session:

| Variable | Default | Face |
|----------+---------+------|
| =my/font-default= | JetBrains Mono | =default= and =fixed-pitch= |
| =my/font-variable-pitch= | Source Sans 3 | =variable-pitch= |
| =my/font-default-height= | 120 (= 12 pt) | absolute height of =default= |

=my/font-default-height= is the only absolute height in the
configuration. Every other face is relative (=1.0=), which is what lets
=text-scale-adjust= and =default-text-scale-mode= scale a buffer
coherently instead of moving one face out from under the others.

*** Per silo

=my/font-silo-styles= gives each silo a family and says whether body
text is proportional:

| Silo | =:family= | =:body= | Reads as |
|------+-----------+---------+----------|
| =journal/= | Playpen Sans Hebrew | =proportional= | handwriting, body included |
| =pks/= | Source Sans 3 | =proportional= | a book page |
| =docu/= | JetBrains Mono | =monospace= | a manual, uniformly machine-set |

Both halves act on the *same face*. =:family= is remapped onto
=variable-pitch= buffer-locally, and =:body proportional= additionally
turns on =variable-pitch-mode=, which makes =default= inherit
=variable-pitch= in that buffer. So the family reaches headings and
quotations in every silo, and body text only where the silo asks.

That is what makes =docu= work: the family is the monospaced one, so
headings, body and code are all machine-set, while a =pks= note sets
the identical structure in a text sans.

=:height= is a relative float compensating for the fact that a
proportional face at the same nominal size usually reads smaller than a
monospaced one.

*** Why =variable-pitch= and not a new face

Because Modus already routes everything through it.
=modus-themes-headings= asks for =variable-pitch= at every level,
=org-quote= inherits it, and =modus-themes-mixed-fonts= keeps tables,
blocks and inline code on =fixed-pitch= regardless. Remapping one face
redresses a whole buffer, and a source block inside a handwritten
journal entry stays monospaced without anything saying so.

*** Matching is by directory prefix

Against the *start* of the visited file name, using the silo variables
from =00-core.el=. The previous test was a substring search for
=journal= anywhere in the path, which matched
=~/notes/pks/20260101T090000--journal-app.org=.

A note in no listed silo — the =~/notes/= root, =captures.org= — keeps
the session defaults.

*** This has no effect on exported PDFs

Faces are a screen concern. =my/org-export-to-pdf= emits LaTeX and
hands it to lualatex, which knows nothing about faces. The journal font
in a PDF comes from =\setmainfont{Playpen Sans Hebrew}= in the
=journal-article= class built by =my/--latex-preamble=
(=16-org-export.el=).

Note the two different criteria: the screen font is chosen by
*directory*, the LaTeX class by the =:journal:= *filetag*. For journal
notes they agree, because journal notes carry that tag. For =pks= and
=docu= there is nothing to disagree about yet — both export through
=article=. Giving them distinct PDF fonts means adding classes in
=16-org-export.el=, not editing =my/font-silo-styles=.

*** An uninstalled family fails silently

Emacs falls back rather than signalling, so a typo produces a
wrong-looking session, not an error. Check with =C-u C-x == over the
text in question, or =M-x describe-font=.

** What used to go wrong

*Every heading in every note was handwritten.* =03b-fonts.el= set the
*global* =variable-pitch= face to Playpen Sans Hebrew, on the
understanding that journals were the only place a proportional font
appeared. They were not: =modus-themes-headings= asks for
=variable-pitch= at levels 1–3 and =org-quote= inherits it. A
documentation note and a thesis chapter rendered their headings as
handwriting. The fix is to make the global face a neutral text sans
and remap it per silo, which is what =my/font-silo-styles= does.

*Dark mode was blocked by custom.el, not by the theme.*
=custom-set-faces= writes the =user= theme, which outranks every real
theme by design. About twenty Org faces were pinned there to literal
light colours — =org-block= on =#fef8e0=, =org-link= on =#555555= — so
=modus-vivendi-tinted= loaded correctly and was then overwritten face by
face. Roughly two thirds of those specs needed no replacement at all:
twelve existed only to add =:inherit fixed-pitch=, which is exactly what
=modus-themes-mixed-fonts= does and it was already enabled.

*=11-org-appearance.el= erased attributes it claimed to preserve.* It
called =set-face-attribute= with =:foreground 'unspecified= on seven Org
faces, under a comment saying this left the colour to =custom.el=. It
does the opposite: =unspecified= *erases* the attribute.

That also produced the impression that *each file looked different*. It
did not — each *frame* did. =set-face-attribute= alters the realised
faces of frames that already exist; a new frame recomputes from the
theme and =custom-set-faces=, where the erasure does not appear. A note
opened with =C-c n v f= (detach to frame) therefore showed the theme's
colours while the same note in the main frame showed none.

This is why =my/theme--custom-faces= uses =custom-set-faces= at runtime
rather than =set-face-attribute=: the former reaches frames made later.

*Two settings had stopped meaning anything.*
=modus-themes-org-blocks= was removed from the package in version 4.4.0
and had been setting a variable no code reads; block appearance is now
the =bg-prose-block-*= overrides. =modus-themes-headings= no longer takes
a =rainbow= property in version 5; per-level colour is =fg-heading-N= in
the overrides, which also reaches backgrounds and overlines.

Two further traps worth remembering:

- =~code~= is =org-code= and ~=verbatim=~ is =org-verbatim= — *two
  different faces*. A difference between them reads like a per-document
  quirk until you notice which marker was used.
- Quote blocks only get the =org-quote= face when
  =org-fontify-quote-and-verse-blocks= is non-nil, which =01-ui.el= sets.

** org-modern

=11-org-appearance.el= runs =org-modern= (GNU ELPA), replacing
=org-bullets=. It styles headline stars, tags, TODO keywords,
priorities, timestamps and table rules using text properties rather than
character composition, so the styled text stays editable and searchable.

Two features are off *on purpose*, and turning either on needs a
matching change elsewhere:

- =org-modern-block-name= is nil. =01-ui.el= maps =#+begin_src= to a
  lambda and =#+begin_quote= to a balloon through
  =prettify-symbols-alist=; org-modern styles the same lines by a
  different mechanism, and running both leaves the delimiter
  half-replaced. To go the other way, remove the four =#+begin=/=#+end=
  pairs from =prettify-symbols-alist= first — one or the other, never
  both.
- =org-modern-block-fringe= is nil. The bracket is drawn in the fringe,
  which =10-visual-fill.el= places inside the margins of a centred
  column and =34-appearance.el= widens for padding; it would float at an
  unpredictable distance from what it brackets. The block background
  from =09-theme.el= marks blocks instead.

=org-tags-column= is 0 and =org-auto-align-tags= nil: with tags rendered
as labels, a right-aligned column becomes a gap between headline and
label.

Labels depend on =line-spacing=, which =11-org-appearance.el= sets to
0.2 — inside the 0.1–0.4 range upstream recommends. Raising it
misaligns them.

** Theme and switching

Two themes, one switch: =modus-operandi-tinted= (light, ochre base) and
=modus-vivendi-tinted= (dark, night sky). See [[#theme][Theme (F5 / C-c u t)]].

** If a colour looks wrong

With point on the text, =C-u C-x == names every face at that position.
=M-x describe-face= then shows where the face got its attributes — in
particular whether the =user= theme (that is, =custom.el=) is
involved, which is the first thing to check when a theme "does not
work".

If a face differs between two frames, that is the symptom described
above and means something has started calling =set-face-attribute=
again.

* Theme (F4 / F5 / C-c u)
:PROPERTIES:
:CUSTOM_ID: theme
:END:

Around a hundred themes, browsable, with the choice surviving a
restart.

Module: =modules/09-theme.el=

** Keys

| Key | Command | What it does |
|-----+---------+--------------|
| =<f4>= | =my/theme-cycle-light= | next light theme |
| =<f5>= | =my/theme-cycle-dark= | next dark theme |
| =S-<f4>= | =my/theme-cycle-all= | next of everything installed |
| =C-c u t= | =my/theme-toggle= | light ↔ dark, remembering the last of each |
| =C-c u T= | =my/theme-select= | pick by name, favourites first |
| =C-c u m= | =my/theme-favourite-toggle= | mark or unmark the current theme |
| =C-c u M= | =my/theme-cycle-favourites= | cycle only the marked ones |
| =C-c u C= | =my/theme-classify= | file the current theme as light or dark |
| =C-c u R= | =my/theme-repair= | clear =user= face overrides and reload |
| =C-c n v t= | =my/theme-toggle= | same, from the View menu |
| =C-c n v y= | =my/theme-select= | same, from the View menu |

*The intended workflow.* Walk everything with =S-<f4>=, press =C-c u m=
on anything worth a second look, then compare just those with =C-c u M=.
The point of browsing a hundred themes is to end up with four.

Cycling is sequential and wraps around, not random: comparing themes
needs an order that can be retraced. The name is echoed on every step
so a good one can be written down.

** Installed collections

| Package | Character |
|---------+-----------|
| =modus-themes= | Prot. Maximum legibility, WCAG AAA. The configured default. |
| =ef-themes= | Prot. Colourful and opinionated, still legible. |
| =standard-themes= | Prot. The stock Emacs look, made consistent. |
| =doric-themes= | Prot. Minimalist: little colour, structure carried by weight and spacing. |
| =doom-themes= | ~60 ports from other editors. Uneven, and the widest net. |

Dropping one is a matter of deleting its =use-package= form. The lists
are filtered through =custom-available-themes=, so a theme from an
uninstalled package disappears from the cycle rather than signalling.

** How light and dark are told apart

Three sources, merged:

1. Variables the packages publish (=ef-themes-light-themes= and
   friends), read through =boundp= so a renamed or absent variable is
   skipped rather than breaking startup.
2. =my/theme-known-light= and =my/theme-known-dark=, curated lists for
   packages that publish nothing — doom-themes in particular.
3. Anything filed by hand with =C-c u C=, stored in
   =my/ui-state-file=.

Everything is filtered through =custom-available-themes=. *That filter
is what makes a wrong name harmless* — it is dropped, not signalled —
and it is why the curated lists can afford to be generous rather than
verified one by one. A theme on neither list still appears in
=S-<f4>= and can be filed from there.

** What survives a theme load, and how

A theme reassigns every face it covers, so anything set outside it has
to be re-applied. =my/theme-after-load-hook= fires for *any* theme
from any family, through =enable-theme-functions= (Emacs 29+, with a
fallback to the Modus hook). =03b-fonts.el= adds =my/fonts-apply=
there, because =default= is one of the faces a theme reassigns.

Add to it sparingly. *A face that inherits from a face the theme
styles needs no hook at all* — which is how the shared faces in
=34-appearance.el= work, and why there is no runtime
=custom-set-faces= block here any more. See [[#org-faces][Org Faces and Fonts]].

** Padding and theme loading

=my/theme-load= disables =spacious-padding-mode= across a theme load
and re-enables it afterwards, rather than refreshing it from the
post-load hook.

The hook fires *during* =load-theme=, when the palette is only half in
place. spacious-padding writes a =:box= onto the mode line computed
from theme colours, and a theme that leaves one =unspecified= — several
doom-themes and the built-in themes do — yields an invalid face
specification. The =face-spec-set= error then aborts =enable-theme=
partway, so the theme applies to some faces and not others: the
"loads, but not completely" symptom.

Both steps are wrapped in =condition-case=. A theme that still cannot
be combined with padding reports itself and leaves padding off,
instead of signalling out of a keypress. Browsing a hundred themes
means meeting a few broken ones.

** When a theme keeps loading wrong

=C-c u R= (=my/theme-repair=). An invalid specification written into
the =user= theme breaks *every subsequent* theme load, not just the
one that produced it, because the =user= theme is re-applied on every
=enable-theme=. The command disables it and reloads.

Nothing here relies on the =user= theme: colour lives in the palette
overrides and the shared faces inherit.

** Editing colour

See [[#org-faces][Org Faces and Fonts]] for the palette-override mechanism and the
=faint= preset the Modus list is built on. In brief: name palette
entries, not hex values, and reload the theme after changing anything
— overrides take effect on load, not on assignment.

** Diagnostics

=M-x my/theme-debug= reports the seven fields that decide how the
frame looks: active theme, frame background, =default= face
background, frame font, =variable-pitch= family, default height,
whether padding is on, and whether org-modern loaded.

The useful pair is =frame-bg= against =default-face-bg=. They are two
different things that normally agree; when they disagree, something
has written the frame parameter directly and a frameset restore is the
usual culprit. =theme=NONE= means no theme is enabled at all, however
plausible the colours look.

** Why the theme loads from emacs-startup-hook

It is the last point in startup: after init.el has loaded every
module, and after =after-init-hook= where the desktop restores its
frameset. A theme is the last word on how a face looks and should get
the last word chronologically too. Loading it at module position 09
put it ahead of two dozen modules that define faces and of a frameset
carrying stale frame colours.

Not =after-init-hook=: that is where the desktop restore itself runs,
and hook order within it is not something this module should have to
reason about.

* Appearance layer (C-c u)
:PROPERTIES:
:CUSTOM_ID: appearance
:END:

The cosmetic layer, plus the interactive controls for size and width.
Nothing here changes what a command does; removing
=34-appearance.el= from =init.el= leaves a working but plainer Emacs.

Modules: =modules/34-appearance.el= (padding, faces, motion),
=modules/03b-fonts.el= (size), =modules/10-visual-fill.el= (width)

** The full C-c u prefix

| Key | Command | Scope |
|-----+---------+-------|
| =C-c u t T m M C R= | theme commands | see [[#theme][Theme]] |
| =C-c u p= | =my/toggle-spacious-padding= | frame |
| =C-c u f= | =my/font-size-adjust= | base size, all frames |
| =C-c u w= | =my/text-width-adjust= | this buffer |
| =C-c u s= | =my/text-scale-save-to-note= | writes front matter |
| =C-c u + - 0= | =text-scale-*= | this buffer only |
| =C-M-= / C-M--= | =default-text-scale-*= | all frames |

"u" for UI. It was the only free single letter under =C-c=: =v= is
git, =w= is workspace, =t= is tabs.

** Adjusting the base font size — C-c u f

Enters a repeat map. =+= and =-= change the size by half a point, =0=
returns to the value compiled into =03b-fonts.el=, and any other key
leaves. The chosen size is written to =my/ui-state-file= immediately,
so it survives a restart without editing a file.

This changes =my/font-default-height=, the size everything else is
relative to — distinct from =text-scale-adjust= (=C-c u + - 0=), which
scales one buffer. Both are useful and they compose: a note carrying
=#+text_scale: 2= stays two steps above whatever the base becomes.

** Adjusting the text width — C-c u w

Also a repeat map. =+= and =-= by five characters, =0= back to what
the normal rules give this buffer, =d= adopts the current width as the
global default, =s= writes it into this note's front matter.

*Nothing is written anywhere until =d= or =s=.* Until then the change
is buffer-local and can be abandoned by leaving the map.

*A reminder about the unit.* The number is CHARACTERS of the buffer's
default face, and visual-fill-column turns it into margins by
multiplying by the width of one character. A pks note in a
proportional face and a docu note in JetBrains Mono look different at
the same number. Adjust each kind separately and expect the figures to
differ; that is the unit doing its job.

** spacious-padding

Frame border, window dividers, fringes as page margins, and a mode
line drawn as a hairline overline instead of a filled bar. The largest
visual change in the layer and the cheapest, being entirely
presentational. =C-c u p= turns it off when screen space is tight.

=spacious-padding-subtle-mode-line= is =t=, not the face-plist form.
The plist takes the foreground of two named faces, which is
=unspecified= under themes that do not set them — see the padding note
in [[#theme][Theme]].

*Check this against notes.* visual-fill-column centres text by
widening the margins, and with
=visual-fill-column-fringes-outside-margins= nil the fringes sit
between margin and text. The =:fringe-width= here therefore *adds* to
the gap. If notes read as too narrow, lower it in
=spacious-padding-widths= before touching the width.

** lin

=02-editing.el= enables =global-hl-line-mode=, giving every buffer the
same highlight at the same intensity. The line means two different
things: a reminder of where point is in a note, and *the selection
itself* in the inbox review queue, the maintenance reports, the
Readwise lists and the dashboards.

=lin= remaps the highlight face buffer-locally in the modes named by
=lin-mode-hooks= — =tabulated-list-mode-hook= covers all four — and
leaves prose buffers to =hl-line= as the theme styles it.

** pulsar

A brief pulse on the line point just landed on. Navigation here is
mostly jumps — a =denote:= link, a backlink, a dashboard entry, a
search hit — each putting point somewhere in a window that has just
been redrawn.

Emacs has no animation engine. This is a timer redrawing one line's
background a few times, and that is the honest ceiling: nothing in
this layer offers transitions, blur or sliding panels, because the
display engine does not have them.

** Shared faces

Six the dashboards use instead of hex values in =propertize=:
=my/dashboard-title=, =-section=, =-note=, =-tag-button=, =-tags=,
=-hint=. Two more for the mode line, defined where their segment is:
=my/modeline-word-count= (=01-ui.el=) and =my/writing-mode-indicator=
(=13-centered-writing.el=).

All eight inherit and name no colour. A literal
='(:foreground "#888888")= in a =propertize= call is correct under one
theme and muddy under the other, and can only be found later by
grepping for hex digits; a named face is greppable, appears in
=M-x list-faces-display=, and can be customised without editing code.

The dashboard faces live in =34-appearance.el= rather than
=15-workspace.el= so neither module needs the other — an undefined
face renders as =default= rather than signalling.

** What was considered and rejected

| Package | Why not |
|---------+---------|
| =doom-modeline= | would need the word-count and pin segments rebuilt as doom segments; icon-heavy look pulls against this direction |
| =centaur-tabs= | buffer tabs, competing with the workspace model =23-fixed-tabs.el= builds on =tab-bar= |
| =modern-tab-bar= | the right tool, but on neither GNU ELPA nor MELPA — =:ensure t= fails at startup |
| =beacon= | overlaps =pulsar=, and configures itself with a literal hex colour |
| =mini-frame= | child frames are the least reliable part of Wayland/PGTK, and it would sit between which-key and every transient menu |
| =all-the-icons= | superseded by =nerd-icons=; neither installed, both needing a font added through the NixOS configuration |

* Per-note appearance
:PROPERTIES:
:CUSTOM_ID: per-note-appearance
:END:

Two front-matter keywords override the defaults for one note.

| Keyword | Effect | Written by |
|---------+--------+------------|
| =#+text_width: 110= | text column for this note, in characters | =C-c u w= then =s= |
| =#+text_scale: 2= | size, in steps of =text-scale-adjust= | =C-c u s= |

Both are read from =org-mode-hook=, so they apply on opening.

** Precedence

Width: =#+text_width:= → the =:docu:= width (=my/fill-column-docu=) →
the global default (=my-fill-column=, itself overridable by =C-c u w
d=).

Scale: applied on top of whatever the base size is. *Relative steps,
not an absolute size* — a note meant to be slightly larger stays
slightly larger after the global size changes. An absolute value would
have to be revisited every time =my/font-default-height= moved.

** Why front-matter keywords and not a property drawer

Because that is the channel this configuration already uses for
per-note facts: =#+schema:=, =#+project:=, =#+wellbeing:=. The setting
then travels with the file through Syncthing, git and any other editor
that reads front matter.

** How they are read

=my/note-keyword= and =my/note-keyword-number= (=00-core.el=) scan the
first four kilobytes with a regexp. Deliberately not
=org-collect-keywords=: these run from =org-mode-hook=, before Org has
finished setting the buffer up, and must not depend on Org internals
being ready. =10-visual-fill.el= already scanned for =#+filetags:= the
same way.

A malformed value yields nil and the default, so a typo in a note
degrades quietly instead of breaking the buffer.

=my/note-keyword-set= writes: it replaces an existing line, or walks
the opening run of =#+keyword:= lines and inserts after it so the new
key joins the front matter rather than landing in the prose. *It does
not save the buffer* — the change is visible and the decision to keep
it is yours.

* Persisted UI state
:PROPERTIES:
:CUSTOM_ID: ui-state
:END:

=~/.emacs.d/ui-state.el=, a plist. Choices made interactively, which
belong to the session rather than to the configuration.

Module: =modules/00-core.el=

| Key | Set by |
|-----+--------|
| =:theme= | any theme load |
| =:theme-light= / =:theme-dark= | the last of each, for =C-c u t= |
| =:themes-favourite= | =C-c u m= |
| =:themes-light= / =:themes-dark= | =C-c u C= |
| =:font-height= | =C-c u f= |
| =:fill-column= | =C-c u w= then =d= |

Not =custom-set-variables=, which would write custom.el — the file
that was emptied on purpose, and reintroducing machine-written
settings there is how the theme became unfixable the first time. Not
the desktop either: this has to work when no session is restored.

Read once at startup, written on every change. *Safe to delete*: every
reader falls back to a compiled-in default. Deleting it resets the
theme to the first light one, the font to the size in
=03b-fonts.el= and the width to =my-fill-column=.

* Session and desktop diagnostics
:PROPERTIES:
:CUSTOM_ID: desktop-diagnostics
:END:

=desktop-save-mode= writes the session from =kill-emacs-hook=, where a
failure is close to invisible: the message scrolls past as the frame
disappears, and the next start quietly restores whatever was last
written successfully.

Module: =modules/01-ui.el=

| Command | Reports |
|---------+---------|
| =my/desktop-status= | whether the file exists, when it was last written, who owns the lock, this Emacs's PID |
| =my/desktop-force-save= | saves now, with errors shown rather than swallowed |

** The frozen-session failure

=desktop-load-locked-desktop= is =check-pid=, not =t=. With =t= a
locked desktop is loaded without asking — but on exit =desktop-save=
still finds the lock held by another PID and declines to overwrite it.
A session that died without releasing its lock (a crash, a =kill -9=,
an init that errored badly enough to be killed) therefore leaves every
later session reading the same frozen state and writing nothing back:
new tabs open, and are gone after a restart.

=check-pid= loads when the owning process is no longer running and
takes ownership, so the next save succeeds. A genuinely concurrent
second Emacs is still refused, which is the case the lock exists for.

The symptom to recognise: =my/desktop-status= naming a lock owner that
is not this Emacs, or a "last written" timestamp older than the last
time the session was closed.

** A frameset overrides what init already set

=desktop-restore-frames= is =t=, and the frameset carries frame
parameters — =background-color=, =foreground-color=, =font=,
=tool-bar-lines=, =vertical-scroll-bars=. Restoring it re-applies
whatever those were when the desktop was last written, on top of
whatever init has already done. Setting them in =default-frame-alist=
is not a defence; frameset restore overrides that too.

Two things re-assert themselves afterwards:
=my/restore-chrome-preferences= on =desktop-after-read-hook= (tool bar
and scroll bar off), and the theme, loaded from =emacs-startup-hook=
which runs strictly later.

* Capture Filing
:PROPERTIES:
:CUSTOM_ID: capture-filing
:END:

How a capture joins a thread that already exists, and what promotion
makes of the result.

Module: =modules/06-capture.el=

** The problem

A capture usually continues something already noted. Retyping its
heading from memory is how near-duplicates appear: =Do poprawy w moim
systemie notowania Emacs= against the same words with one letter
different is two headings to Org — and two separate notes once each is
promoted, which is the failure =C-c n c m= exists to prevent.

** The prompt

=C-c C-c= in a capture buffer asks one question with three answers:

: File under (TAB for existing, text for a new heading, empty for none):

| Answer | Result |
|--------+--------|
| =TAB= onto an existing heading, then =RET= | text is appended under it with a =Source:= line and *no second heading* |
| type something, then =RET= | the capture keeps its own heading, named with what was typed |
| =RET= on an empty prompt | the capture keeps its own heading, untitled — exactly as before |

=RET= submits *what was typed*, not the highlighted candidate, so a new
heading beginning like an existing one can be entered. =TAB= steps onto
the list. This is the same behaviour as the tag prompts
([[#keyword-prompts][Keyword Prompts]]) and is driven by the same
=my/notes-keyword-preselect=, deliberately not duplicated under a second
name. =C-TAB= inserts without exiting; there is no =,= binding here,
because this prompt reads one heading and a heading may contain a comma.

Naming happens by editing the capture buffer while it is still open, so
Org writes the headline out itself.

The prompt does not appear on =C-c C-k= (abort) or =C-c C-w= (refile,
which already chooses a destination itself).

Only headings with text are offered as destinations — an unnamed capture
was never given a subject and cannot serve as one.

** The source line

One format, in captures.org and in promoted notes alike:

: Source: [[denote:20260715T101500][Przewodnik po bibliografii]] — [2026-08-02 nie 18:10]

A property drawer is not an option for a fragment filed under an
existing heading: Org only recognises one directly beneath a heading.
Body text is also what a note receives, so all three places share this.

Duplicate detection compares the *bracket link*, not the whole line, so
the same origin captured at two different times counts as one source.

*Limitation:* a line of prose beginning with =Source:= is read as
metadata. Indent it or rephrase it.

** What promotion does with it

=my/capture--split-into-segments= turns a capture subtree into an
ordered list of (source, text) pairs. The heading's own =:SOURCE:= and
=:CAPTURED:= properties describe the text before the first =Source:=
line in the body — so an entry captured the old way parses as one
segment and *nothing in captures.org needs migrating*.

The note then receives the segments in capture order, with a =Source:=
line emitted only when the origin changes. Nothing is reordered or
merged.

** Why the C-c C-c hook is the fragile part

org-capture fixes an entry's destination before the buffer opens and
offers no way to change it at the end. The entry is therefore filed
normally and moved afterwards, from
=org-capture-prepare-finalize-hook= (ask) and
=org-capture-after-finalize-hook= (move).

That depends on org-capture's internal sequence. Three guards make every
failure harmless:

1. The entry to move must be the last level-2 heading under =Ideas=;
2. its =:CAPTURED:= property must equal the one read from the capture
   buffer moments earlier;
3. the destination heading must still exist, and its marker is taken
   *before* anything is deleted.

Any of these failing produces a message and leaves the capture exactly
where org-capture put it. If the message =Capture left in place: …=
appears, nothing was lost.

Heading text, when some was typed, is kept as the first paragraph of the
appended fragment.

* One symbol, one owner
:PROPERTIES:
:CUSTOM_ID: symbol-collisions
:END:

Elisp has a single namespace for functions. A second =defun= of the same
name replaces the first silently, and the module that loses is the one
whose author never opened the other file.

This has already happened once. =my/denote--silo-files= was defined in
=05-notes.el= taking a directory and in =27-denote-identifiers.el=
taking nothing; =init.el= loads 27 after 05, so moving a note between
silos failed with =Wrong number of arguments=. =my/denote--file-identifier=
was duplicated in the same two files with matching behaviour, which
hides the collision instead of reporting it.

** The check

#+begin_src sh
grep -h "^(defun \|^(defmacro \|^(defsubst " modules/*.el \
  | sed 's/^(def[a-z]* //; s/[ )].*//' | sort | uniq -d
#+end_src

Should print nothing. Worth running before committing any helper with a
generic name.

** The other half: called but never defined

A symbol defined twice is one way a module can be wrong while still
loading. A symbol *called* and never defined is the other, and it is
quieter: parens balance, the file loads, and the error appears only when
the one key that uses it is pressed. That is how
=my/maintenance--set-keywords= reached 26-maintenance.el without
existing.

Byte-compilation reports it:

#+begin_src sh
emacs -Q --batch \
  --eval '(dolist (f (file-expand-wildcards "modules/*.el")) (byte-compile-file f))' \
  2>&1 | grep "not known to be defined" | grep "my/"
rm -f modules/*.elc
#+end_src

Filtering on =my/= is what makes it readable: under =-Q= the compiler
knows nothing about Denote, Vertico or Org either, and those warnings
are noise. A warning naming a =my/= symbol is not.

** What neither check covers

Both are about *symbols*: one defined twice, one never defined. Neither
sees a *value* of the wrong type, and that is the other way a module
breaks.

=tabulated-list-print-entry: Wrong type argument: stringp, nil= in the
inbox review came from =(file-name-directory "")=, which returns nil
rather than the empty string. It had been in the code for as long as the
module had, and fired the first time an inbox note lacked a
=:source_path:= — the first note created there rather than migrated in.
Byte-compilation says nothing about it.

Two habits help where the checks cannot:

- *Guarantee a type at the boundary that demands it,* not upstream of it.
  =tabulated-list-mode= signals on a non-string and takes the whole list
  down with it, so one malformed note makes a thousand good ones
  unreachable. Coercing each cell turns that into one blank field on one
  visible row.
- *Read what a function returns, not only what it is given.* The broken
  line already had an =(or ... "")= — guarding the argument, never the
  result.


The same over =defvar=, =defconst= and =defcustom= prints only
=vertico-preselect=, which is deliberate: =05-notes.el= and
=06-capture.el= each carry a bare =(defvar vertico-preselect)= to mark a
variable belonging to Vertico as special for that file. That declares
rather than defines, and repeating it is harmless.

** Shared helpers and their owners

| Helper | Owner | Also called from |
|--------+-------+------------------|
| =my/denote--file-identifier= | =27-denote-identifiers.el= | 05-notes.el, 25-inbox-review.el |
| =my/notes-read-keywords= | =05-notes.el= | 06-capture.el, 24-readwise.el |
| =my/notes--completion-keys= | =05-notes.el= | 06-capture.el |
| =my/fixed-tab-goto= | =01-ui.el= | 15-workspace.el, 21-dashboards.el |
| =my/transient-append= | =12-transient.el= | every module adding a menu entry |

Resolution is at call time, so load order does not constrain these —
only whether the owning module is loaded at all. Callers that would
otherwise produce a void-function backtrace guard with =fboundp= and
fail with a sentence instead.

** Still outstanding

Reading Denote front matter is implemented independently in
=15-workspace.el=, =21-dashboards.el=, =25-inbox-review.el=,
=28-writing-projects.el=, =16-org-export.el= and =29-writing-export.el=,
each with its own regexp for =^#+title:=. The names differ, so nothing
collides, but it is the same duplication one step short of the failure
above.

* Maintenance (C-c n !)
:PROPERTIES:
:CUSTOM_ID: maintenance
:END:

Looking after the collection rather than writing in it: finding what the
migration broke, and repairing it.

Module: =modules/26-maintenance.el=

** The menu

| Key | Command | Does |
|-----+---------+------|
| =i= | =my/denote-check-identifiers= | notes sharing an identifier (27-denote-identifiers.el) |
| =g= | =my/denote-check-signatures= | notes sharing a Folgezettel signature |
| =b= | =my/denote-check-broken-links= | =denote:= links whose target does not exist |
| =l= | =my/denote-find-self-links= | notes linking to their own identifier |
| =k= | =my/maintenance-keyword-inventory= | every keyword with a usage count |
| =z= | =my/maintenance-notes-without-keywords= | notes carrying no keywords |
| =R= | =my/denote-change-identifier= | change this note's identifier *and repoint every link to it* |
| =I= | =my/denote-fix-duplicates= | walk duplicate identifier groups, one confirmation each |
| =K= | =my/maintenance-rename-keyword= | rename or remove one keyword across the collection |

Lower case reads, upper case writes. Anything that renames files across
the collection is on a capital so a mistyped key cannot start it.

** Changing an identifier

=R= is the answer to "this journal entry's time is wrong and I know what
it should be". It verifies the new identifier is free in the silos,
rewrites =#+identifier:=, renames the file, and *updates every =denote:=
link pointing at it* across the whole notes tree, inbox included —
links first, so an interruption leaves links pointing at a file that
still exists.

It accepts any valid identifier, not only the suggested next free one,
so the remembered time can be typed directly.

Denote will not do this itself. Its manual gives sample code for finding
duplicate identifiers and states that, as an edge case, it is not part
of the code base; and =denote-rename-file-using-front-matter= never
changes an identifier, because Denote treats the file name as the source
of truth for that field.

** Renaming a keyword everywhere

=K= asks for the keyword and its replacement, then opens a review list
of every note that uses it. Nothing is written until a decision is made
on a row.

| Key | Effect |
|-----+--------|
| =RET= | preview the note in a side window, focus stays on the list |
| =o= | step into the preview, where it scrolls normally (=q= returns) |
| =y= | rename this note |
| =n= | leave it alone |
| =g= | redraw |
| =q= | leave; unhandled notes keep their keyword |

Focus stays on the list on =RET= for the same reason as in
[[#menu-inbox][Inbox Review]]: the decision keys live in the list buffer, and
=view-mode= in the preview binds =n= to its own command. Press =o= when
you want to read the note properly.

Each row shows its state — pending, renamed, skipped — and the mode line
counts what is left. Leaving part-way through is safe: every note is
written and saved as it is handled, so there are never modified buffers
waiting.

An empty replacement removes the keyword. Renaming onto an *existing*
keyword merges two spellings, which is why the replacement prompt
completes over the keywords already in use.

Only the keyword field is written — title, identifier and signature stay
as they are.

*Why a buffer and not a prompt.* The first version asked file by file
with =read-multiple-choice=, offering =v= to view the note. That reads
raw input events: every mouse movement redrew the prompt, and while it
was reading, no other window received input — so the note it had just
displayed could not be scrolled. A blocking prompt owns the input stream
by definition and cannot be made to allow navigation.

** Finding what needs renaming

=k= lists every keyword with the number of notes using it, *sorted
alphabetically rather than by frequency*: near duplicates are what a
migrated collection accumulates, and =filozofia= directly above
=flozofia= is what makes them visible. The count is the second signal,
and keywords used once are marked with =·=. Each keyword is a button
that starts a rename of it.

** Scope: inbox yes, attachments no

Everything here reads =my/denote--all-files= from
=27-denote-identifiers.el=, which walks =my-notes-dir= directly and
returns =.org= files only.

*The staging inbox is included.* A keyword renamed everywhere except
there would come back one note at a time as notes are filed — a failure
surfacing weeks later, attached to nothing. Denote's own file listing
would have excluded it, since =25-inbox-review.el= sets
=denote-excluded-directories-regexp= to ="inbox"=.

*Attachments are not.* A PDF or an image carries an identifier so links
can reach it, but has no front matter and no keywords to review.

** Why denote-explore is not used for keywords

The first version of this module wrapped =denote-explore=, which does
cover keywords. In this collection its writing commands produced a
second file holding only front matter while leaving the original
untouched, and carried out changes that had been declined at the prompt.

Neither was reproducible from the outside and both may depend on the
state of this particular tree, so this is not a verdict on the package —
it is a decision about what can be vouched for with three thousand real
notes on the other end of the command.

** How a keyword is written

Against Denote's *file-naming scheme*, not against its functions. Two
attempts to call them failed on argument count —
=denote-rename-file-keywords=, then =denote-rename-file= — because those
signatures have changed across releases, and correcting a call only
moves the failure to the next one.

The scheme does not change; it is what Denote exists to promise:

: IDENTIFIER[==SIGNATURE][--TITLE][__KEYWORD_KEYWORD].EXTENSION

together with =#+filetags:  :one:two:= holding the same keywords.
Changing a keyword means editing those two places, in that order: front
matter first, file name second. Interrupted in between, the note has its
new keywords and its old name — visible, and repaired by running the
same rename again.

*Nothing before the keyword field is touched.* Identifier, signature and
title are carried across as text, so — unlike =denote-rename-file=,
which rebuilds the whole name — no second change arrives alongside the
one that was asked for.

The command refuses rather than guesses in the two cases where guessing
would lose work: a buffer with unsaved changes, and a destination name
already taken.

The edit goes to the *file*, not through whatever buffer is visiting it.
A buffer can be read-only for reasons that have nothing to do with the
file — =view-mode=, which the preview here turns on — and writing to a
file should not need that buffer's permission. An open buffer is
re-read afterwards with =revert-buffer=, preserving its modes and its
read-only state.

Keywords are lower-cased and underscores and spaces become hyphens.
That is not Denote's full sluggification and does not try to be:
keywords here are normally chosen from the completion list of those
already in use.

Keyword sorting is not lost with =denote-explore-sort-keywords= gone:
Denote sorts keywords on every rename while =denote-sort-keywords= is
non-nil, so the commands above maintain it note by note.

* Global Keybindings
:PROPERTIES:
:CUSTOM_ID: global-keybindings
:END:

Bindings set outside the transient menu itself. Audited against every
=global-set-key=/=define-key=/=:bind= in the repo as of this revision —
=modules/08-keybindings.el= carries a hand-maintained copy of this
table (=my/show-keybindings-help=, =C-c h k=), including the transient
menu contents; re-audit both together if bindings change, rather than
hand-editing either from memory.

| Binding  | Function                     | Set in module                |
|----------+-------------------------------+-------------------------------|
| C-c n    | =my/notes-menu=               | =12-transient.el=            |
| C-c n z  | =my/zettelkasten-menu=        | =22-zettelkasten.el= (appended) |
| C-c c    | =org-capture=                 | =06-capture.el=              |
| C-c x    | =my/zotero-menu=              | =18-zotero-transient.el=     |
| C-c b c  | =my/cite-check-keys=          | =17-bibliography.el=         |
| C-c r s  | =my/readwise-sync=            | =24-readwise.el=             |
| C-c r r  | =my/readwise-review=          | =24-readwise.el=             |
| C-c r o  | =my/readwise-open-directory=  | =24-readwise.el=             |
| C-c X    | =my/inbox-extract=            | =25-inbox-review.el= (inbox notes only) |
| C-c p    | =my/org-export-to-pdf=        | =16-org-export.el=           |
| C-c d f  | =denote-open-or-create=       | =08-keybindings.el=          |
| C-c d l  | =denote-link=                 | =08-keybindings.el=          |
| C-c d b  | =denote-backlinks=            | =08-keybindings.el=          |
| C-c d r  | =denote-rename-file=          | =08-keybindings.el=          |
| C-c d t  | =denote-rename-file-keywords= | =08-keybindings.el=          |
| C-c d s  | =my/desktop-save-now=         | =01-ui.el=                   |
| C-c d k  | =my/desktop-toggle-keep-buffer= | =01-ui.el=                 |
| C-c t n  | =tab-bar-new-tab=             | =01-ui.el=                   |
| C-c t c  | =tab-bar-close-tab=           | =01-ui.el=                   |
| C-c t o  | =tab-bar-switch-to-tab=       | =01-ui.el=                   |
| C-c t r  | =tab-bar-rename-tab=          | =01-ui.el=                   |
| C-c w d  | =my/open-notes-dashboard=     | =15-workspace.el=            |
| C-c w x  | =my/notes-explore=            | =15-workspace.el=            |
| C-c w r  | =denote-explore-random-note=  | =15-workspace.el=            |
| C-c a k  | =keyfreq-show=                | =14-typing-analytics.el=     |
| C-c a s  | =keylog-status=               | =14-typing-analytics.el=     |
| C-c a t  | =keylog-disable=              | =14-typing-analytics.el=     |
| C-c a T  | =keylog-enable=               | =14-typing-analytics.el=     |
| C-x g    | =magit-status=                | =07-git.el=                  |
| C-x M-g  | =magit-dispatch=              | =07-git.el=                  |
| C-c g s  | =magit-status=                | =07-git.el=                  |
| C-c g l  | =magit-log-current=           | =07-git.el=                  |
| C-c g b  | =magit-blame=                 | =07-git.el=                  |
| C-c v s  | =my/notes-git-status=         | =07-git.el=                  |
| C-c v c  | =my/commit-notes-now=         | =07-git.el=                  |
| C-c v S  | =my/config-git-status=        | =07-git.el=                  |
| C-c v C  | =my/commit-config-now=        | =07-git.el=                  |
| C-c v d  | =magit-diff-buffer-file=      | =07-git.el=                  |
| C-c v h  | =magit-log-buffer-file=       | =07-git.el=                  |
| C-;      | =flyspell-correct-wrapper=    | =03-spelling.el= (flyspell-mode-map) |
| C-c f b  | =my/spell-check-visible=      | =03-spelling.el=             |
| C-a      | =mark-whole-buffer=           | =02-editing.el= (rebind)     |
| C-f      | =isearch-forward=             | =02-editing.el= (rebind)     |
| C-s      | =save-buffer=                 | =02-editing.el= (rebind)     |
| C-z      | =undo=                        | =02-editing.el= (rebind)     |
| C-x C-b  | =ibuffer=                     | =02-editing.el=              |
| C-x u    | =vundo=                       | =02-editing.el=              |
| C-S-z    | =undo-redo=                   | =02-editing.el=              |
| M-Q      | =my/unfill-region=            | =02-editing.el=              |
| C-c o i  | =open-init-el-bottom-split=   | =08-keybindings.el=          |
| C-c e b  | =eval-buffer=                 | =08-keybindings.el=          |
| C-c e r  | =eval-region=                 | =08-keybindings.el=          |
| C-c h k  | =my/show-keybindings-help=    | =08-keybindings.el=          |
| C-c s    | =my/spell-correct-previous=   | =08-keybindings.el=          |
| C-c S    | =my/spell-add-previous-to-dict= | =08-keybindings.el=        |
| C-c m c  | =my/notes-create-menu=        | =08-keybindings.el=          |
| C-c m w  | =my/toggle-writeroom=         | =08-keybindings.el=          |
| C-c m k  | =my/denote-keywords-edit=     | =08-keybindings.el= (duplicate of C-c d t) |
| <f4>     | =my/theme-cycle-light=        | =09-theme.el=                |
| <f5>     | =my/theme-cycle-dark=         | =09-theme.el=                |
| S-<f4>   | =my/theme-cycle-all=          | =09-theme.el=                |
| C-c u t  | =my/theme-toggle=             | =09-theme.el=                |
| C-c u T  | =my/theme-select=             | =09-theme.el=                |
| C-c u m  | =my/theme-favourite-toggle=   | =09-theme.el=                |
| C-c u M  | =my/theme-cycle-favourites=   | =09-theme.el=                |
| C-c u C  | =my/theme-classify=           | =09-theme.el=                |
| C-c u R  | =my/theme-repair=             | =09-theme.el=                |
| C-c u p  | =my/toggle-spacious-padding=  | =34-appearance.el=           |
| C-c u f  | =my/font-size-adjust=         | =03b-fonts.el= (all frames)  |
| C-c u s  | =my/text-scale-save-to-note=  | =03b-fonts.el=               |
| C-c u w  | =my/text-width-adjust=        | =10-visual-fill.el=          |
| C-c u +  | =text-scale-increase=         | =34-appearance.el= (this buffer) |
| C-c u -  | =text-scale-decrease=         | =34-appearance.el= (this buffer) |
| C-c u 0  | =my/text-scale-reset=         | =34-appearance.el=           |
| C-M-=    | =default-text-scale-increase= | =34-appearance.el= (all frames) |
| C-M--    | =default-text-scale-decrease= | =34-appearance.el= (all frames) |
| C-x b    | =consult-buffer=              | =01-ui.el=                   |
| M-g g    | =consult-goto-line=           | =01-ui.el=                   |
| M-g i    | =consult-imenu=               | =01-ui.el=                   |
| M-s l    | =consult-line=                | =01-ui.el=                   |

*On the consult bindings:* the usual setup rebinds =C-s= to
=consult-line=. It is not done here — =C-s= is =save-buffer= — and
=C-x C-b= is =ibuffer=, so =consult-buffer= took the free =C-x b=
instead. Buffer-local search is =M-s l=; searching across notes stays
=consult-denote-grep= (=C-c n f g=).

*On the =C-c u= prefix:* "u" for UI. It was the only free single letter
in the =C-c= space that suggested the right thing; =C-c v= is git,
=C-c w= is workspace, =C-c t= is tabs. The commands are defined in the
module each one drives, not gathered in one place — see
[[#appearance][Appearance layer]] and [[#theme][Theme]].

=C-c u f= and =C-c u w= each enter a repeat map, so =+= and =-= keep
working without the prefix until any other key is pressed.

*Note:* there is no =C-c F ...= spelling prefix — an earlier revision
of this table and of =08-keybindings.el= documented one that was never
actually bound anywhere.

Of the spelling commands, =correct-previous= and =add-to-dict= now have
direct bindings (=C-c s= and =C-c S=); =check-full-buffer= and =toggle=
still exist only inside the transient menu — see
[[#menu-spelling][Tools → Spelling]] above. The menu entries =C-c n s=
and =C-c n a= remain as well: they carry =:transient t= and so leave
the menu open, which suits correcting several words in a row, while the
direct bindings suit a single fix.

* NuPhy Air75 V2 — Caps Lock layer
:PROPERTIES:
:CUSTOM_ID: nuphy-layer
:END:

Six firmware macros on a momentary layer held by Caps Lock. They are
stored in the keyboard's EEPROM and are *not* reproducible from any
repository — this table is the only written record, so update it here
whenever the layer changes.

| Key | Types    | Command                          |
|-----+----------+----------------------------------|
| =M= | C-c m c  | =my/notes-create-menu=           |
| =F= | C-c d f  | =denote-open-or-create=          |
| =S= | C-c s    | =my/spell-correct-previous=      |
| =A= | C-c S    | =my/spell-add-previous-to-dict=  |
| =W= | C-c m w  | =my/toggle-writeroom=            |
| =L= | C-c d l  | =denote-link=                    |

Everything here is firmware: the macros type ordinary chords, so the
same commands work identically from the ThinkPad's built-in keyboard,
just with more keystrokes. Nothing in this configuration depends on the
NuPhy being attached.

** How the layer is triggered
:PROPERTIES:
:CUSTOM_ID: nuphy-layer-trigger
:END:

Caps Lock carries =MO(4)= on the Win base layer (layer 2 — the physical
Win/Mac switch selects which layer is the base, so none of this is
active in Mac mode). =MO()= emits no HID report at all, which is why
the =ctrl:nocaps= XKB rule in the NixOS configuration never fires for
this keyboard even though it is still set: there is no
=KEY_CAPSLOCK= scancode for it to rewrite. The physical left Control is
an ordinary Control key.

The consequence on the ThinkPad's built-in keyboard, where =MO()=
cannot exist, is that Caps Lock is a second Control there. Capital-lock
itself is reached with both Shift keys together
(=shift:both_capslock=).

Raw HID access for the VIA web configurator comes from
=modules/system/hardware/keyboard-qmk.nix= in the NixOS repository,
scoped to USB vendor =19f5=. Factory reset is =Fn + [=, which clears
every macro in the table above.

** Why these six
:PROPERTIES:
:CUSTOM_ID: nuphy-layer-rationale
:END:

Chosen from =keyfreq= output (=C-c a k=) rather than by guesswork.
=my/spell-correct-previous= was the second most-invoked command in the
configuration and had no binding at all; =my/notes-menu= was first.

An earlier draft of the layer also carried a navigation cluster
(arrows, word jumps, Home/End, PgUp/PgDn on the right hand). It was
dropped: the Air75 V2 is a 75 % board with a real arrow cluster and
dedicated navigation keys, so the layer was duplicating keys that
already exist under the same hand. That reasoning does not carry over
to 60 % boards, where the cluster would be worth having.
